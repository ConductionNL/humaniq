/.idea/
/*.iml

/vendor/
/vendor-bin/*/vendor/

/.php-cs-fixer.cache
/tests/.phpunit.cache
/.phpunit.cache
.phpunit.cache/

/node_modules/
/js/
/.widget-probe-tmp-*/
/custom_apps/
/config/

/coverage/
/coverage-frontend/
/coverage-vitest/

# Quality reports
/quality-reports/
quality-baseline.json

# PHPCS/Psalm output files (not config files)
phpcs-*.json
phpcs-*.txt
psalm-errors.json
psalm-output.json

# Hydra builder internal state — emitted by run-hydra-gates.sh
task-audit.json

# Playwright e2e run artefacts. `.auth/` holds a real logged-in session for the
# instance under test — never commit it.
/tests/e2e/.auth/
/tests/e2e/playwright-report/
/tests/e2e/test-results/
/tests/e2e/.last-cj-run.txt
/playwright-report/
/test-results/

# Agent/test scratch and tool caches — generated, never source.
# Added by the 2026-08-25 fleet hygiene sweep (ADR-100 Decision 2).
.stale/
/.e2e-state/
.phpunit.result.cache
