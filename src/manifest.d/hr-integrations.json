{
	"pageInstances": [
		{
			"templateRef": "indexScaffold",
			"register": "humaniq",
			"schema": "IntegrationAccount",
			"params": {
				"id": "IntegrationAccounts",
				"route": "/configuratie/integraties",
				"title": "Integrations",
				"columns": [
					"name",
					"purpose",
					"status",
					"reviewedAt"
				],
				"sort": {
					"field": "name",
					"order": "asc"
				},
				"filters": [
					"status"
				],
				"description": "A record of which external systems have access to this app's data, what they use it for, and when that access was last reviewed. This page documents access. It does not grant or revoke it: the actual permissions live in OpenRegister. Administrators only.",
				"actions": [
					{
						"id": "create-integration-account",
						"label": "Register integration",
						"type": "open-form",
						"icon": "PlusCircleOutline",
						"register": "humaniq",
						"schema": "IntegrationAccount",
						"onSuccessRoute": "IntegrationAccountDetail",
						"successMessage": "Integration registered. Configure the actual RBAC access and the app password in OpenRegister / Nextcloud."
					}
				]
			}
		},
		{
			"templateRef": "singleDataScaffold",
			"register": "humaniq",
			"schema": "IntegrationAccount",
			"params": {
				"id": "IntegrationAccountDetail",
				"route": "/configuratie/integraties/:id",
				"title": "Integration",
				"dataIcon": "PowerPlugOutline",
				"gridHeight": 6,
				"note": "hris-api-public (2026-07-18): a governance/audit record of one external system granted access to humaniq via OpenRegister's public REST surface. The 'Integratie' data widget carries the whole record (name/purpose/nextcloudUserId/grantedSchemas/status/reviewedBy/reviewedAt/createdAt); no exclude, because IntegrationAccount carries NO $ref field (nextcloudUserId is a plain string per ADR-062 rule 7, never a $ref to an Employee), so there is no Related panel to resolve. NO lifecycleActions: status is a plain actief/ingetrokken enum with no x-openregister-lifecycle transition map (the OrgUnitDetail/ShiftDetail precedent) -- toggling it records a governance state, it does not run a guarded workflow, and it changes NOTHING about actual access (design.md D2). No stat/files/calendar/comms leaves -- a catalog record has no numeric headline aggregate, document, appointment, or correspondence thread. Audit history stays a sidebar tab so a reviewer can see who changed the catalog record and when. Alleen-beheerder surface (like DsrRequestDetail); enforcement is server-side OpenRegister RBAC, not a manifest flag."
			}
		}
	]
}
