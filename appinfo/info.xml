<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>humaniq</id>
    <name lang="en">Humaniq</name>
    <name lang="nl">Humaniq</name>
    <summary lang="en">Open-source HR and payroll administration with built-in labour-law and wage-tax compliance.</summary>
    <summary lang="nl">Open-source HR- en salarisadministratie met ingebouwde arbeidsrecht- en loonheffingencompliance.</summary>
    <description lang="en"><![CDATA[Open-source HR and payroll administration for Dutch and EU employers, built on the OpenRegister data layer.

Employees log hours and submit expense claims through a submit → approve/reject workflow. The underlying register also holds Employee, EmploymentContract, PayrollRun, Payslip and WageTaxFiling records, which a static, versioned corpus of international HR/labour and payroll rules (EU labour directives, ILO conventions, GDPR for employee data, occupational health & safety, national labour law, and payroll / wage-tax & social-security compliance) is audited against by an executable RuleEngine that reports compliance coverage.

**Key Features**

- **Timesheets** — per-employee, per-period hour registration with a submit → approve/reject/reopen approval workflow
- **Expense claims** — submit → approve/reject → reimbursed workflow for employee expense claims
- **OpenRegister integration** — Employee / EmploymentContract / Payslip / PayrollRun / WageTaxFiling objects live in the OpenRegister `hrmq` register
- **Compliance rule engine** — auto-discovered per-domain CheckProviders enforce the machine-checkable rule corpus
- **`occ humaniq:rules:audit`** — reports enforced ÷ machine-checkable coverage, violations, and per-object-type compliance
- **`occ humaniq:rules:seed-testdata`** — idempotently seeds compliant local test data
- **Full quality pipeline** — PHPCS, PHPMD, Psalm, PHPStan

Free and open source under the EUPL-1.2 license.

**Support:** For support, contact support@conduction.nl.
    ]]></description>
    <description lang="nl"><![CDATA[Open-source HR- en salarisadministratie voor Nederlandse en EU-werkgevers, gebouwd op de OpenRegister-datalaag.

Medewerkers registreren uren en dienen declaraties in via een indienen → goedkeuren/afkeuren-workflow. Het onderliggende register houdt ook Employee-, EmploymentContract-, PayrollRun-, Payslip- en WageTaxFiling-gegevens vast, die een statisch, geversioneerd corpus van internationale HR/arbeids- en loonheffingenregels toetst via een uitvoerbare RuleEngine die de compliance-dekking rapporteert.

**Belangrijkste functies**

- **Urenregistratie** — per-medewerker, per-periode urenregistratie met een indienen → goedkeuren/afkeuren/heropenen-workflow
- **Declaraties** — indienen → goedkeuren/afkeuren → vergoed-workflow voor declaraties van medewerkers
- **OpenRegister-integratie** — Employee-, EmploymentContract-, Payslip-, PayrollRun- en WageTaxFiling-objecten leven in het OpenRegister `hrmq`-register
- **Compliance-regelengine** — automatisch gedetecteerde CheckProviders per domein handhaven het machine-controleerbare regelcorpus
- **`occ humaniq:rules:audit`** — rapporteert gehandhaafd ÷ machine-controleerbaar dekkingspercentage, overtredingen en compliance per objecttype
- **`occ humaniq:rules:seed-testdata`** — vult idempotent lokale testdata die aan de gehandhaafde regels voldoet

Vrij en open source onder de EUPL-1.2-licentie.

**Ondersteuning:** Voor ondersteuning, neem contact op via support@conduction.nl.
    ]]></description>
    <version>0.2.0-unstable.20260826203457</version>
    <licence>EUPL-1.2</licence>
    <author mail="info@conduction.nl" homepage="https://www.conduction.nl/">Conduction</author>
    <namespace>Humaniq</namespace>
    <documentation>
        <user>https://github.com/ConductionNL/humaniq</user>
        <admin>https://github.com/ConductionNL/humaniq</admin>
        <developer>https://github.com/ConductionNL/humaniq</developer>
    </documentation>
    <category>organization</category>
    <website>https://github.com/ConductionNL/humaniq</website>
    <discussion>https://github.com/ConductionNL/humaniq/issues</discussion>
    <bugs>https://github.com/ConductionNL/humaniq/issues</bugs>
    <repository>https://github.com/ConductionNL/humaniq</repository>

    <screenshot>https://raw.githubusercontent.com/ConductionNL/humaniq/development/img/app-store.svg</screenshot>

    <dependencies>
        <!-- ORDER IS LOAD-BEARING. info.xsd declares <dependencies> as a
             SEQUENCE: php, database, command, lib, owncloud, nextcloud,
             architecture, backend. With <nextcloud> first, xmllint rejects the
             file with "Element 'php': This element is not expected. Expected
             is one of ( architecture, backend )" — the elements are both
             valid, only their order was not. -->
        <php min-version="8.3"/>
        <nextcloud min-version="32" max-version="34"/>
        <!-- App dependency (not expressible in app-info.xsd): OpenRegister.
             Humaniq owns no database tables — Employee / EmploymentContract /
             PayrollRun / Payslip / WageTaxFiling / Timesheet / Expense objects
             all live in the OpenRegister `hrmq` register (imported via
             OCA\Humaniq\Repair\InitializeRegister), and the Vue manifest pages
             read/write that register directly through the
             @conduction/nextcloud-vue object store (src/manifest.json
             declares "dependencies": ["openregister"]).

             THE REGISTER SLUG IS DELIBERATELY STILL `hrmq`. OpenRegister's
             ImportHandler::autoCreateRegisterIfApplication() looks the register
             up by slug (derived from x-openregister.app in
             lib/Settings/humaniq_register.json). Renaming the slug alongside
             the app id would not move the register — it would create a second,
             empty one and orphan every existing employee, contract, payslip
             and payroll run. See the comment in humaniq_register.json. -->
    </dependencies>

    <!-- ORDER IS LOAD-BEARING, and this file had it wrong three ways. info.xsd
         declares the body of <info> as a SEQUENCE, so these blocks must appear
         as: dependencies, background-jobs, repair-steps, commands, navigations.
         <navigations> used to sit here (before <repair-steps>) and
         <background-jobs> sat after it, which is why xmllint rejected the file
         with "Element 'repair-steps': This element is not expected. Expected is
         one of ( contactsmenu, collaboration, openmetrics, sabre, trash,
         versions, external-app )" — once <navigations> is consumed, the schema
         will accept nothing that legally precedes it. Nothing here changed but
         the order. -->

    <background-jobs>
        <!-- Monthly statutory + bovenwettelijk leave accrual onto every active
             employee's holiday LeaveBalance (leave-accrual-job). Fires ~daily;
             the per-balance lastAccruedPeriod marker — not the interval —
             defines "once a month". Gated by the leave_accrual_enabled toggle. -->
        <job>OCA\Humaniq\BackgroundJob\LeaveAccrualJob</job>
    </background-jobs>

    <repair-steps>
        <!-- Order here is a SEQUENCE too: pre-migration, post-migration,
             live-migration, install, uninstall. <install> used to come first,
             which the schema rejects.

             THE TWO Migrate* STEPS CARRY THE `hrmq` -> `humaniq` APP-ID RENAME
             AND MUST RUN FIRST IN BOTH BLOCKS. Nextcloud namespaces both
             `oc_appconfig` and `oc_preferences` by app id, so the rename makes
             every previously stored value unreachable — the new id is simply a
             different app. MigrateAppConfigKeys must precede InitializeRegister
             because InitializeRegister (via SettingsService) writes the imported
             register/schema-id bookkeeping keys itself; if it ran first those
             keys would already exist under `humaniq` and the migration would
             skip them as "already present", stranding the old values. -->
        <post-migration>
            <!-- Copy every stored IAppConfig value from the `hrmq` namespace. -->
            <step>OCA\Humaniq\Repair\MigrateAppConfigKeys</step>
            <!-- Copy every per-user preference from the `hrmq` namespace. -->
            <step>OCA\Humaniq\Repair\MigrateUserPreferences</step>
            <!-- Third leg of the rename, and the one that lives on the
                 OpenRegister register ROW. The register slug moves too
                 (`hrmq` -> `humaniq`), together with `x-openregister.app`,
                 which for a `type: application` configuration IS a register
                 slug rather than an attribution label. It MUST run before
                 InitializeRegister: OpenRegister resolves a register by slug
                 and its not-found branch CREATES a second, empty register
                 rather than failing, so an import that ran first would fork the
                 register and leave every stored object behind on the old row,
                 silently.

                 It moves no data. An object is bound to its register by NUMERIC
                 id — `_register` in every shard table, and the tables are named
                 `oc_openregister_table_<registerId>_<schemaId>` — so this is one
                 column on one row. Idempotent and non-destructive. -->
            <step>OCA\Humaniq\Repair\MigrateRegisterSlug</step>
            <!-- SIBLING OF MigrateRegisterSlug, AND NEITHER COVERS THE OTHER.
                 OpenRegister resolves the two halves of a configuration by
                 DIFFERENT keys: a REGISTER by slug alone
                 (RegisterMapper::find), a SCHEMA by the PAIR
                 (SchemaMapper::findByApplicationAndSlug(slug, application)).
                 This app now passes `appId: Application::APP_ID` = `humaniq`,
                 while every schema it already owns still carries
                 `application = hrmq` — so the pair matches nothing, and
                 ImportHandler's not-found branch is not an error path, it is
                 the "create a new one" path. The import below would build a
                 SECOND, EMPTY schema set under the new application id while
                 every stored object stayed bound to the old rows. Nothing
                 errors; the app renders empty collections.

                 MUST run before InitializeRegister, for the same reason
                 MigrateRegisterSlug does — the import is the thing that forks.

                 Measured on a live install: 2 schemas still under `hrmq`, and
                 zero of their slugs collide with a twin under `humaniq`, so
                 both move.

                 Where a slug DOES already have a twin under the new
                 application id it refuses rather than merges: two rows sharing
                 (application, slug) means findByApplicationAndSlug(), which
                 caps at one row, silently picks one and the other's objects
                 become unreachable. That is a decision about data, not a
                 migration.

                 Never deletes a schema, and never throws — it runs under
                 <install>, where an escaping exception aborts the install and
                 the app never enables. -->
            <step>OCA\Humaniq\Repair\MigrateSchemaApplicationId</step>
            <!-- Import the hrmq register (lib/Settings/humaniq_register.json +
                 register.d/*.json fragments) into OpenRegister. Idempotent;
                 fails soft when OpenRegister is not installed. -->
            <step>OCA\Humaniq\Repair\InitializeRegister</step>
            <!-- hrmq-asset-fleet-merge (tasks.md section 13, blocking defect):
                 rewrite pre-existing Asset/AssetAssignment objects from the
                 old Dutch dialect (category/status enum values, kenteken/
                 serienummer/aanschafdatum/aanschafwaarde, uitgifteDatum/
                 innameDatum/uitgifteBonSigned/eigenBijdrage) to the renamed
                 one — the schema rename above never touches an object that
                 already exists (OpenRegister's seed import is create-only).
                 Runs AFTER InitializeRegister so the new-dialect schema is
                 already imported. Idempotent; fails soft when OpenRegister is
                 not installed. post-migration (not live-migration) so it also
                 re-runs on `occ maintenance:repair`, not only on a real app
                 upgrade — see the step's own docblock. -->
            <step>OCA\Humaniq\Repair\MigrateAssetDialect</step>
            <!-- hours-process-redesign (Decision 9): backfill the Timesheet
                 identity caches (userId/managerUserId/administrationId),
                 synthesize one origin:"migration" TimeEntry per legacy
                 timesheet with hours but no entries, and recompute the
                 aggregates through the same TimesheetAggregationService the
                 runtime listener uses. Runs AFTER InitializeRegister so the
                 TimeEntry schema is already imported. Idempotent (re-runs
                 create nothing); one summary line per run; fails soft when
                 OpenRegister is not installed. -->
            <step>OCA\Humaniq\Repair\MigrateHoursProcess</step>
        </post-migration>
        <install>
            <!-- SAME steps as post-migration, and that duplication is the point.
                 Nextcloud's Installer::installAppLastSteps() guards BOTH the
                 pre- and post-migration blocks with `if ($previousVersion !== '')`
                 — on a FRESH install `installed_version` is still empty, so
                 neither block runs. Only `repair-steps/install` is executed
                 unconditionally.

                 THIS IS THE PATH THE RENAME ACTUALLY TAKES. Nextcloud discovers
                 `humaniq` as a DIFFERENT app from `hrmq`, so enabling it is a
                 fresh install with an empty `installed_version` — the
                 post-migration block above never fires and the two Migrate*
                 steps would never run if they were declared only there.

                 The app previously declared post-migration ONLY, so on every
                 fresh install the register import never ran at all:
                 `occ app:enable hrmq` printed "hrmq 0.2.0 enabled" and NOT ONE
                 line of that step's own output, the `hrmq` register was absent
                 from OpenRegister, and the SPA fell back to its default route on
                 every page — the shape the CI E2E suite hit as 68 failures whose
                 two "passes" were both the default route.

                 Every step here is idempotent and fails soft, so running them on
                 install AND on upgrade is safe; whichever fires first wins and
                 the other is a version-unchanged skip. -->
            <step>OCA\Humaniq\Repair\MigrateAppConfigKeys</step>
            <step>OCA\Humaniq\Repair\MigrateUserPreferences</step>
            <!-- Third leg of the rename, and the one that lives on the
                 OpenRegister register ROW. The register slug moves too
                 (`hrmq` -> `humaniq`), together with `x-openregister.app`,
                 which for a `type: application` configuration IS a register
                 slug rather than an attribution label. It MUST run before
                 InitializeRegister: OpenRegister resolves a register by slug
                 and its not-found branch CREATES a second, empty register
                 rather than failing, so an import that ran first would fork the
                 register and leave every stored object behind on the old row,
                 silently.

                 It moves no data. An object is bound to its register by NUMERIC
                 id — `_register` in every shard table, and the tables are named
                 `oc_openregister_table_<registerId>_<schemaId>` — so this is one
                 column on one row. Idempotent and non-destructive. -->
            <step>OCA\Humaniq\Repair\MigrateRegisterSlug</step>
            <!-- SIBLING OF MigrateRegisterSlug, AND NEITHER COVERS THE OTHER.
                 OpenRegister resolves the two halves of a configuration by
                 DIFFERENT keys: a REGISTER by slug alone
                 (RegisterMapper::find), a SCHEMA by the PAIR
                 (SchemaMapper::findByApplicationAndSlug(slug, application)).
                 This app now passes `appId: Application::APP_ID` = `humaniq`,
                 while every schema it already owns still carries
                 `application = hrmq` — so the pair matches nothing, and
                 ImportHandler's not-found branch is not an error path, it is
                 the "create a new one" path. The import below would build a
                 SECOND, EMPTY schema set under the new application id while
                 every stored object stayed bound to the old rows. Nothing
                 errors; the app renders empty collections.

                 MUST run before InitializeRegister, for the same reason
                 MigrateRegisterSlug does — the import is the thing that forks.

                 Measured on a live install: 2 schemas still under `hrmq`, and
                 zero of their slugs collide with a twin under `humaniq`, so
                 both move.

                 Where a slug DOES already have a twin under the new
                 application id it refuses rather than merges: two rows sharing
                 (application, slug) means findByApplicationAndSlug(), which
                 caps at one row, silently picks one and the other's objects
                 become unreachable. That is a decision about data, not a
                 migration.

                 Never deletes a schema, and never throws — it runs under
                 <install>, where an escaping exception aborts the install and
                 the app never enables. -->
            <step>OCA\Humaniq\Repair\MigrateSchemaApplicationId</step>
            <step>OCA\Humaniq\Repair\InitializeRegister</step>
            <!-- hours-process-redesign: <install> is the ONLY hook that runs
                 unconditionally on a fresh install (see above) — without this
                 entry a fresh install would seed legacy-shaped timesheets and
                 never migrate them. Runs AFTER InitializeRegister; on fresh
                 installs it finds the seed rows and behaves identically. -->
            <step>OCA\Humaniq\Repair\MigrateHoursProcess</step>
        </install>
    </repair-steps>

    <commands>
        <!-- Audit the register's HR/labour objects against the machine-checkable
             rule corpus (lib/Standards/RuleCatalogue + RuleEngine). Read-only. -->
        <command>OCA\Humaniq\Command\RulesAuditCommand</command>
        <!-- Idempotently backfill local TEST data to satisfy the enforced rules
             (provider sample objects + provider field defaults). Test/dev. -->
        <command>OCA\Humaniq\Command\RulesSeedTestDataCommand</command>
        <!-- On-demand, humaniq-only re-run of the Asset/AssetAssignment dialect
             migration (hrmq-asset-fleet-merge, tasks.md section 13) also
             wired as the MigrateAssetDialect post-migration repair step.
             Idempotent; prints per-schema inspected/rewritten/alreadyCurrent/
             skipped counts and every skip reason. -->
        <command>OCA\Humaniq\Command\AssetsMigrateDialectCommand</command>
        <!-- Post each approved PayrollRun as a balanced loonjournaalpost into
             shillinq's JournalEntry register (payroll-glpost-shillinq MVP
             trigger). Duck-typed no-op when shillinq is absent. -->
        <command>OCA\Humaniq\Command\GlPostRunCommand</command>
        <!-- Hand off each payable PayrollRun's payslips as a draft SEPA
             PaymentRun into shillinq's PaymentRun register
             (payroll-sepa-netpay-shillinq MVP trigger). Duck-typed no-op when
             shillinq is absent. -->
        <command>OCA\Humaniq\Command\NetPayRunCommand</command>
        <!-- Generate standard HR documents (arbeidsovereenkomst, aanbiedingsbrief,
             werkgeversverklaring, getuigschrift) via docudesk's template/rendering
             engine (humaniq-docudesk-documents MVP trigger). Duck-typed no-op
             (skipped-no-docudesk) when docudesk is absent. -->
        <command>OCA\Humaniq\Command\DocumentsGenerateCommand</command>
        <!-- Extract receipt fields via docudesk's FinancialExtractionService
             and prefill still-empty Expense amount/expenseDate/vendor/
             vatAmount fields (receipt-ocr MVP trigger). Duck-typed no-op
             (skipped-no-docudesk) when docudesk is absent; never overwrites a
             human-entered value or changes Expense.status. -->
        <command>OCA\Humaniq\Command\ExpenseExtractReceiptCommand</command>
        <!-- Sync approved leave and sickness absence onto the configured
             shared Nextcloud calendar (leave-calendar-nc MVP trigger).
             Duck-typed no-op (skipped-no-calendar) when the calendar is not
             configured or cannot be resolved. -->
        <command>OCA\Humaniq\Command\CalendarSyncCommand</command>
        <!-- Create (or, with the recalculate option) the draft PayrollRun + engine Payslips
             for a wage period via the table-driven NL payroll calculator
             (payroll-core-engine MVP trigger). Draft-only; approval stays a
             human act. -->
        <command>OCA\Humaniq\Command\PayrollRunCommand</command>
        <!-- Run-scoped corpus audit over one period's PayrollRun(s) + their
             payslips (payroll-core-engine); exits non-zero on any mandatory
             violation. -->
        <command>OCA\Humaniq\Command\PayrollVerifyCommand</command>
        <!-- Recompute a sealed Payslip from its stored engineInputSnapshot
             (never the live Employee/EmploymentContract state) and compare
             cents-exact against the archived figures (audit-trail-payroll,
             fixing humaniq#98); exits non-zero on any mismatch or missing
             snapshot. -->
        <command>OCA\Humaniq\Command\PayrollReproduceCommand</command>
        <!-- Print (and optionally persist) the run-to-run payroll mutation
             report — who joined/left and each shared employee's per-component
             cents delta between two PayrollRuns (payroll-mutation-reports).
             Pure read over persisted payslips; never recomputes. -->
        <command>OCA\Humaniq\Command\PayrollMutationsCommand</command>
        <!-- Support-facing mirror of POST /api/payroll/proforma — simulate a
             gross-to-net breakdown from CLI flags (proforma-payslip MVP
             trigger). Persists nothing. -->
        <command>OCA\Humaniq\Command\ProformaPayslipCommand</command>
        <!-- Compute (and, with the apply option, settle) a terugwerkende
             kracht (TWK) correction for a sealed prior-period payslip as a
             delta — never mutates the sealed original (retro-adjustments
             MVP trigger). -->
        <command>OCA\Humaniq\Command\PayrollAdjustCommand</command>
        <!-- Annual tax-year roll preflight: asserts the new nl-YYYY.json
             table exists and reports the data-only, period-derived design
             (retro-adjustments). Changes no engine state. -->
        <command>OCA\Humaniq\Command\PayrollYearTransitionCommand</command>
        <!-- On-demand Arbeidstijdenwet cross-check over one Roster (by id or
             period) and its RosterAssignments, regardless of publish status
             (rostering MVP). Read-only; exits non-zero on any mandatory
             violation. -->
        <command>OCA\Humaniq\Command\RosterCheckCommand</command>
        <!-- Batch-effectuate every approved, due CompAdjustment in one
             compensation review cycle (comp-cycles MVP trigger): writes the
             new grossMonthlySalary onto the Employee and drives the
             effectuate transition. The dry-run option evaluates without writing. -->
        <command>OCA\Humaniq\Command\CompEffectuateCommand</command>
        <!-- Settle one approved LeaveTransaction (buy/sell leave hours,
             leave-buy-sell MVP trigger): idempotent, adjusts only
             LeaveBalance.bovenwettelijkHours and stamps the transaction
             settled, entitledHours/usedHours are never touched. -->
        <command>OCA\Humaniq\Command\LeaveBuySellSettleCommand</command>
        <!-- Compute/persist the idempotent per-(administration, year)
             werkkostenregeling vrije-ruimte assessment (wkr-administration
             MVP trigger): rolls the cross-object fiscale loonsom + WKR
             declarations into WkrAssessment. The all option iterates every
             distinct (administration, year) pair found across the payslips. -->
        <command>OCA\Humaniq\Command\WkrAssessCommand</command>
        <!-- AVG data-subject-rights orchestration over OpenRegister's DsarService
             (avg-dsr MVP trigger): export (Art 15 inzage / Art 20 portabiliteit),
             retention-guarded erase preview/execute (Art 17 vergetelheid), and
             rectify (Art 16). Each requires the as-user option to establish the
             privileged session DsarService::assertPrivileged() demands. -->
        <command>OCA\Humaniq\Command\AvgDsrExportCommand</command>
        <command>OCA\Humaniq\Command\AvgDsrEraseCommand</command>
        <command>OCA\Humaniq\Command\AvgDsrRectifyCommand</command>
        <!-- offer-esign: generate the offer-letter (aanbiedingsbrief) PDF and
             raise a docudesk e-signature request for one Application in
             status "aanbod". KNOWN LIMITATION: docudesk's
             SigningService::createRequest() throws "No authenticated user"
             from a genuine occ CLI process; the ApplicationDetail manifest
             action is the primary, actually-working write trigger. -->
        <command>OCA\Humaniq\Command\OfferRequestSignatureCommand</command>
        <!-- offer-esign: read-only poll of docudesk signing-request status
             onto Application.offerSigningStatus (getRequest() carries no
             session guard, so this genuinely works from CLI). Never writes
             Application.status or invokes the aannemen transition. -->
        <command>OCA\Humaniq\Command\OfferSyncSignatureCommand</command>
        <!-- interview-scheduling: sync scheduled recruiting Interviews onto
             the configured shared Nextcloud calendar (fork of
             CalendarSyncCommand for a second, separately-configured shared
             calendar). Duck-typed no-op (skipped-no-calendar) when the
             calendar is not configured or cannot be resolved. -->
        <command>OCA\Humaniq\Command\InterviewCalendarSyncCommand</command>
    </commands>

    <navigations>
        <navigation>
            <id>humaniq</id>
            <name>Humaniq</name>
            <route>humaniq.page.index</route>
            <icon>app.svg</icon>
        </navigation>
    </navigations>
</info>
