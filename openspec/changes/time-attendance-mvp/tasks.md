# Tasks — time-attendance-mvp

- [ ] 1. Schema: new fragment `lib/Settings/register.d/hr-attendance.json` with `AttendanceRecord` (employeeId `$ref` Employee / date / clockIn / clockOut / breakMinutes / stored workedHours / location enum / userId NC-uid / status + `x-openregister-lifecycle` open⇄gesloten with sluiten/heropenen carrying-write semantics) per REQ-TA-001; workedHours stored (NOT a calculation) with the REQ-TA-002 description; register `info.version` 0.3.0 → 0.4.0
- [ ] 2. Corpus: add `nl-atw-dagelijkse-rust`, `nl-atw-max-werkdag`, `nl-atw-pauze` (with `parameters.breakTiers`) to `lib/Standards/rules/labour.json` per REQ-TA-003 — framework `nl-arbeidstijdenwet`, sourceUrl `https://wetten.overheid.nl/BWBR0007671`, effectiveDate 1996-01-01; add `nl-arbeidstijdenwet` to the SCHEMA.md framework examples; bump `RuleCatalogue::VERSION` to `2026-07.1`
- [ ] 3. Context: add `buildAttendanceContext()` to `lib/Service/RuleAuditService.php` (per-employee date→{clockIn,clockOut} index over AttendanceRecord, wired into `audit()` as `$context['attendance']`, empty-set degradation when the schema is absent — the buildGlPostContext pattern) per REQ-TA-004
- [ ] 4. Checks: implement `lib/Standards/Checks/NlAttendanceChecks.php` (daily rest via the attendance context, consecutive-day gap ≥ 11h; max werkdag ≤ 12h from raw clock fields; pauze tiers read from rule `parameters`, NOT hard-coded; vacuous pass on null clockOut / missing previous-day sibling) per REQ-TA-004
- [ ] 5. Unit tests: PHPUnit coverage for the three ATW checks — compliant day, 8h-gap rest violation, no-previous-day vacuous pass, 13h max-werkdag violation, open-day vacuous pass, zero-break and 45-min-tier pauze cases, workedHours-independence (REQ-TA-002 scenario) (extend `tests/Unit/`, bootstrap per `tests/bootstrap.php`)
- [ ] 6. Manifest: `AttendanceRecords` child under `VerlofVerzuimGroup` + `MijnAanwezigheid` child under `MijnHrGroup`; pages `AttendanceRecords` (index), `AttendanceRecordDetail` (data excl. employeeId + related + lifecycleActions Sluiten/Heropenen matching hr-attendance.json action-for-action + audit sidebar; no files widget), `MijnAanwezigheid` (`filter: {userId: "@me"}`, mirroring MijnUren); `AttendanceRecord` deepLink per REQ-TA-005; `npm run check:manifest` passes
- [ ] 7. Seed data: four AttendanceRecords (jansen compliant closed day with `userId: "admin"` / devries 07-08+07-09 consecutive pair with 8h overnight gap / bakker 8h zero-break day) per REQ-TA-006 in `lib/Settings/register.d/hr-seed.json` — workedHours exact derivations, placeholders only
- [ ] 8. Quality gates: `composer check:strict` green; run the audit logic (RuleEngine::evaluate with the attendance context) against the seeded AttendanceRecords and confirm exactly the expected violations — devries-0709 → `nl-atw-dagelijkse-rust`, bakker-0710 → `nl-atw-pauze`, nothing else, no pre-existing rule regresses

Acceptance criteria (plain reminders, not tasks):
- Timesheet schema, lifecycle and NoSelfApprovalGuard remain byte-identical — the attendance↔timesheet boundary is documentation only (design D5), no aggregation automation
- `workedHours` is a stored property, never an `x-openregister-calculations` entry; the ATW predicates never read it (REQ-TA-002)
- lifecycle edges exactly as REQ-TA-001 (sluiten/heropenen only — no approval states, no invented edges on the detail page)
- corpus rule ids/framework/sources/sourceUrls exactly as the design.md table; pauze tiers live in rule `parameters.breakTiers`; single severity per rule id (no advisory tier — NlVerzuimChecks precedent)
- `buildAttendanceContext()` follows the existing RuleAuditService builder pattern: private, side-effect free, loaded once per audit run, empty-set degradation
- gate-28: title + description on every new property; SPDX docblocks on the new check provider; `@spec` tags on new/changed methods
- i18n: new page labels/actions use English keys per ADR-007 where keys apply (Dutch display strings follow the existing manifest convention; keep keys stable)
