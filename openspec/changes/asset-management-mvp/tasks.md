# Tasks — asset-management-mvp

- [ ] 1. Fragment: create `lib/Settings/register.d/hr-assets.json` with the `Asset` schema (name/category enum laptop|telefoon|voertuig|gereedschap|toegangspas|kleding|overig/serienummer/kenteken/aanschafdatum/aanschafwaarde nullable/status/active default true; `x-schema-org: schema:IndividualProduct`, icon `PackageVariantClosed`, version 0.1.0) per REQ-AST-001
- [ ] 2. Fragment: add the `x-openregister-lifecycle` on `Asset.status` (initial `beschikbaar`, terminal `afgeschreven`; transitions `uitgeven` beschikbaar→uitgegeven, `innemen` uitgegeven→ingenomen, `vrijgeven` ingenomen→beschikbaar, `afschrijven` beschikbaar|ingenomen→afgeschreven; NO guards, `afschrijven` NOT reachable from `uitgegeven`) per REQ-AST-001
- [ ] 3. Fragment: add the `AssetAssignment` schema to the same fragment (assetId `$ref` Asset + employeeId `$ref` Employee + uitgifteDatum required, innameDatum nullable, uitgifteBonSigned default false, notes; `x-schema-org: schema:OwnershipInfo`, icon `HandshakeOutline`, version 0.1.0; NO lifecycle) per REQ-AST-002 / REQ-AST-003
- [ ] 4. Corpus: add `nl-asset-assignment-consistency` (mandatory) and `nl-asset-inname-bij-offboarding` (recommended) to `lib/Standards/rules/labour.json` (domain labour, jurisdiction NL, framework `hr-assets-core`, machineCheckable true, control-style source per design.md); add the `hr-assets-core` framework slug to `lib/Standards/rules/SCHEMA.md` examples per REQ-AST-004
- [ ] 5. Corpus: bump `RuleCatalogue::VERSION` per SCHEMA.md's rule — re-verify the current HEAD value first (authoring-time `2026-07.5` → `2026-07.6`; parallel changes bump the same constant) per REQ-AST-004
- [ ] 6. Audit context: extend `RuleAuditService::buildRelatedContext()` with the `Asset` index (`byId` map of `{id, status, active}`, the OrgUnit index shape) and the defensive `Offboarding` index (`plannedCompletionByEmployeeId`, latest planned-completion date per employeeId; skip unreadable entries; degrade-to-empty when the schema is absent — vacuous pass, either landing order vs. offboarding-wizard-mvp) per REQ-AST-005
- [ ] 7. Checks: implement `lib/Standards/Checks/NlAssetChecks.php` (CheckProvider, no seeded samples) — both predicates keyed on `AssetAssignment`: consistency (innameDatum>=uitgifteDatum when present; open assignment must resolve assetId to a `uitgegeven` asset and employeeId to a known employee, fail-closed on dangling refs) and inname-bij-offboarding (open assignment + employee's Offboarding planned completion strictly before the audit date; vacuous pass on empty/missing index) per REQ-AST-005
- [ ] 8. Unit tests: PHPUnit coverage in `tests/Unit/` for both predicates (coherent/incoherent dates; open-on-beschikbaar and open-on-ingenomen violate; open-on-uitgegeven passes; closed-on-beschikbaar passes; dangling assetId/employeeId fail closed; offboarding overdue violates; offboarding future/absent/empty-index pass vacuously) and for the `buildRelatedContext()` Asset+Offboarding indexes through `RuleAuditService::audit()` with a fake ObjectService double (extend the existing RuleAuditServiceTest pattern)
- [ ] 9. Manifest: add the `Assets` index page (route `/assets`, columns name/category/status/serienummer, filters category/status, sort name asc) and the `AssetDetail` detail page (data + lifecycleActions exposing exactly uitgeven/innemen/vrijgeven/afschrijven + related + FK-scoped Uitgiftes object-list + `integration: files` Uitgiftebonnen widget + audit sidebar tab) per REQ-AST-006
- [ ] 10. Manifest: add the `AssetAssignments` index page (columns assetId/employeeId/uitgifteDatum/innameDatum/uitgifteBonSigned, sort uitgifteDatum desc) and the `AssetAssignmentDetail` detail page (data excluding assetId/employeeId + related + audit sidebar tab; no lifecycleActions) per REQ-AST-006
- [ ] 11. Manifest: add the two `ExpensesGroup` menu children (Assets `PackageVariantClosed`, Uitgiftes `HandshakeOutline`) after ExpenseApproval WITHOUT renaming the group (IA renames owned by hrmq-ia-navigation-alignment), and the two `deepLinks` entries (`/apps/hrmq/assets/{uuid}`, `/apps/hrmq/asset-assignments/{uuid}`) per REQ-AST-006
- [ ] 12. Seed data: add the three Asset seeds (laptop `uitgegeven`, telefoon `beschikbaar`, voertuig with placeholder kenteken `uitgegeven`) and two AssetAssignment seeds against the slugged employees (`assetassignment-visser-bus` closed/consistent; `assetassignment-jansen-telefoon` open on the `beschikbaar` telefoon — the deliberate violation) to `lib/Settings/register.d/hr-seed.json` per REQ-AST-007
- [ ] 13. Quality gates: `composer lint` + PHPUnit (php:8.3-cli, full suite, no pre-existing test regressed) + `npm run check:manifest` (Ajv PASS, 0 errors) + `npm run build` all green; verify against the seeded fixture values that the audit reports exactly one `nl-asset-assignment-consistency` violation (jansen-telefoon), zero `nl-asset-inname-bij-offboarding` violations, and no regression in pre-existing checks (run `occ hrmq:rules:audit` when a live instance is available, else the standalone RuleEngine-against-seed-fixtures verification the pension change established)

Acceptance criteria (plain reminders, not tasks):

- category enum exactly `laptop|telefoon|voertuig|gereedschap|toegangspas|kleding|overig`; status enum exactly `beschikbaar|uitgegeven|ingenomen|afgeschreven`; enum append later, never rename
- both reference fields are `$ref`s storing UUIDs (ADR-062 rule 7); seeds reference by slug only, and only the slugged employees (`employee-jansen`, `employee-visser`)
- rule ids/framework/severity exactly as the design.md table; severity vocabulary is mandatory/conditional/recommended only
- `nl-asset-inname-bij-offboarding` MUST pass vacuously when the Offboarding schema/objects are absent (parallel offboarding-wizard-mvp lands in either order); if that change lands a different planned-completion field name, follow the landed schema in the index extractor — rule id/statement/predicate shape unchanged
- lifecycleActions expose ONLY the four fragment transitions (no invented transitions — PayrollRunDetail precedent); AssetAssignment has no lifecycle and no lifecycleActions
- bijtelling stays OUT (engine-blocked, Spectr `hrmq-canon-fleet-bijtelling`) — a voertuig is just an Asset with a kenteken; follow-up spec `fleet-bijtelling`
- re-verify `RuleCatalogue::VERSION` and the ExpensesGroup shape against HEAD at apply time (parallel changes touch both); union-merge labour.json/SCHEMA.md/RuleAuditService on conflict and re-validate JSON
- i18n: new page labels use the manifest's existing Dutch-label convention; keys stay stable for `hrmq-i18n-locale-completeness`
