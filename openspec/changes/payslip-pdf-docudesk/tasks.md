# Tasks — payslip-pdf-docudesk

- [ ] 1. Schema: bump `GeneratedDocument` to v0.2.0 in `lib/Settings/register.d/hr-documents.json` — `documentType` enum gains `loonstrook`/`jaaropgaaf` (append-only), new nullable `$ref` fields `payslipId` (Payslip) and `jaaropgaafId` (Jaaropgaaf) per REQ-HDD-001
- [ ] 2. Schema: add `Jaaropgaaf` v0.1.0 to the same fragment (employeeId/year required + the D2 derivable totals, `verrekendeArbeidskorting` nullable, `loonheffingennummer` snapshot, `aggregatedAt`) per REQ-PPD-001
- [ ] 3. Config: add `getDocumentsEmployerLoonheffingennummer()` (config key `documents_employer_loonheffingennummer`, obvious placeholder default) to `lib/Service/SettingsService.php` and extend `getDocumentsEmployerBlock()` per REQ-PPD-002
- [ ] 4. Service: loonstrook generation path in `lib/Service/HrDocumentService.php` — `dataRefs = [Employee, Payslip]`, `contractId` null, `payslipId` recorded, idempotency key (payslipId, documentType) per REQ-PPD-002 / REQ-HDD-002 / REQ-HDD-006; verify the docudesk schema-name-keying against the installed HEAD, not from memory
- [ ] 5. Service: jaaropgaaf aggregation — upsert `Jaaropgaaf` per (employeeId, year) with the D2 sums (`zvwMode == 'inhouding'` filter, `verrekendeArbeidskorting` stays null, config-snapshot loonheffingennummer, `aggregatedAt`), `failed` on an empty year, never duplicate per REQ-PPD-003
- [ ] 6. Service: jaaropgaaf generation path — render `dataRefs = [Employee, Jaaropgaaf]` after the upsert, `jaaropgaafId` recorded, idempotency key (jaaropgaafId, documentType) per REQ-PPD-003 / REQ-HDD-006
- [ ] 7. Service: loonstrook backlog — every Payslip lacking an active loonstrook document, narrowed by period/employee; jaaropgaaf scope — employees with ≥1 payslip in the year per REQ-HDD-007
- [ ] 8. Command: extend `lib/Command/DocumentsGenerateCommand.php` — `--type` accepts the two new values, new `--period`/`--year` options, type-scoped usage guards (`usage-error`, exit 1), default no-option backlog unchanged per REQ-HDD-007
- [ ] 9. Controller: extend `DocumentController::generate()` with optional `payslipId` + `authorizePayslip()` (RBAC-scoped resolve, 404 before any docudesk call; 400 on missing subject param; jaaropgaaf not accepted); NO change to `appinfo/routes.php` per REQ-HDD-008
- [ ] 10. Corpus: add `nl-loonstrook-verplicht` to `lib/Standards/rules/payroll.json` beside `nl-loonstrook-verstrekken` (reporting / NL / nl-loonheffingen / recommended / machineCheckable, BW 7:626) and bump `RuleCatalogue::VERSION` to `2026-07.6` per REQ-PPD-004
- [ ] 11. Checks: extend `lib/Standards/Checks/NlDocumentChecks.php` with the Payslip-keyed predicate and `lib/Service/RuleAuditService.php::buildDocumentsContext()` with the `generatedLoonstrookByPayslip` index per REQ-PPD-004
- [ ] 12. Manifest: `Jaaropgaven` index + `JaaropgaafDetail` pages and the `PayrollGroup` menu child (two-panel detail, no lifecycleActions/files, audit sidebar) per REQ-PPD-005
- [ ] 13. Manifest: `PayslipDetail` api-call action "Genereer PDF" (`{payslipId: "@objectId", documentType: "loonstrook"}`, confirm, pre-translated toasts) and `GeneratedDocumentDetail` `gd-data` exclude += `payslipId`/`jaaropgaafId`; `npm run check:manifest` passes per REQ-HDD-008 / REQ-PPD-005
- [ ] 14. Seed: `jaaropgaaf-jansen-2026` (arithmetically consistent with `payslip-jansen-2026-05`: 3800.00/1102.00/2698.00, payPeriodCount 1) + `gendoc-loonstrook-jansen-2026-05` (`status: generated`, placeholder templateRef/filePath) in `hr-documents.json` per REQ-PPD-005; existing seeds untouched (union, no regression)
- [ ] 15. Unit tests: extend `tests/Unit/Service/HrDocumentServiceTest.php` (mocked container/services per the existing setup) — jaaropgaaf aggregation math over a multi-payslip year incl. the zvwMode filter and year boundary (REQ-PPD-003 scenario), payslip dataRefs assembly (REQ-PPD-002), per-payslip and per-jaaropgaaf idempotency incl. the upsert-not-duplicate case (REQ-HDD-006), and the usage/authorization guards (empty year, jaaropgaaf without --year, loonstrook without payslipId)
- [ ] 16. Quality gates: `composer check:strict` green; in the dev container run the register import, `occ hrmq:documents:generate --type loonstrook` and `--type jaaropgaaf --year 2026` with and without docudesk enabled, the PayslipDetail action end-to-end, and `occ hrmq:rules:audit` — confirm `nl-loonstrook-verplicht` flags an undocumented payslip, passes the seeded one, and no existing rule regresses

Acceptance criteria (plain reminders, not tasks):
- no HTTP path to docudesk anywhere; container-resolved PHP services only, string FQCNs, zero compile-time imports (the leaf's invariant, unchanged)
- no fabricated numbers: `verrekendeArbeidskorting` null, zvw werkgeversheffing never summed as werknemersaandeel
- the four letter types, their backlog, endpoint path, and idempotency keys stay byte-for-byte compatible — no regression
- SPDX + `@spec` tags on every new/changed PHP method (gate-16); i18n keys ENGLISH per ADR-007; no Co-Authored-By trailers
