# Tasks — mss-team-scope

- [x] 1. Schemas: add the optional nullable `managerUserId` string (never a `$ref`; D1 description: denormalized copy of the active-placement unit manager's `nextcloudUserId`, HR/back-office populated alongside `userId`, audited by `nl-mss-manager-consistency`) to `Timesheet` (hr-timesheet.json 0.3.0→0.4.0), `Expense` (hr-expense.json 0.2.0→0.3.0) and `LeaveRequest` (hr-leave.json 0.2.0→0.3.0); bump `lib/Settings/hrmq_register.json` info.version 0.5.0→0.6.0 per REQ-MSS-001
- [x] 2. Manifest pages: add `TeamUrengoedkeuring` (/timesheets/team-approval), `TeamDeclaratiegoedkeuring` (/expenses/team-approval) and `TeamVerlofgoedkeuring` (/leave-requests/team-approval) — each copying its global approval page's config and adding base `filter: { "managerUserId": "@me" }`; global approval pages stay byte-identical per REQ-MSS-002
- [x] 3. Menu: add the three `Team-*goedkeuring` children (icon `AccountCheckOutline`) inside `TimesheetsGroup`/`ExpensesGroup`/`VerlofVerzuimGroup` directly after each global approval entry — NO new top-level menu (ADR-001 Rule 2) per REQ-MSS-002
- [x] 4. Dashboard: re-scope `dash-approve-hours`/`dash-approve-expenses` to `{ "managerUserId": "@me", "status": "submitted" }` (retitle "(mijn team)", re-route to the team pages, ids stable), add `dash-team-approve-leave`, add the HR fallback row `dash-hr-approve-hours`/`dash-hr-approve-expenses` (global counts, routes to the global approval pages), re-flow layout (team 3×4 at Y2, HR 2×6 at Y4, table to Y6) and update the page `_note` per REQ-MSS-003
- [x] 5. Corpus: add `nl-mss-manager-consistency` to `lib/Standards/rules/labour.json` (domain labour, jurisdiction NL, framework `hr-org-core` — existing slug, no SCHEMA.md edit; severity recommended, machineCheckable true, control-style source) and bump `RuleCatalogue::VERSION` 2026-07.5→2026-07.6 per REQ-MSS-004
- [x] 6. Audit context: extend `RuleAuditService::buildRelatedContext()` — OrgUnit index gains `managerId`, Employee index gains `nextcloudUserId`, new `OrgAssignment` index `byEmployeeId` (lists of `{orgUnitId, endDate}`); same single pre-pass, same degrade-to-empty per REQ-MSS-005
- [x] 7. Checks: extend `NlOrgChecks` with the shared manager-consistency predicate registered under `Timesheet`/`Expense`/`LeaveRequest` (violates only on a fully-resolved mismatch against every active placement; vacuous on absent stamp / no active assignment / unmanaged unit / unresolvable manager / manager without NC account; any-match passes) per REQ-MSS-005
- [x] 8. Unit tests: PHPUnit coverage in `tests/Unit/` for the predicate (consistent pass, mismatch violation, each vacuous hop, multiple-active-assignments any-match, inactive-assignment skip) and for the three `buildRelatedContext()` extensions through `RuleAuditService::audit()` with a fake ObjectService double (existing RuleAuditServiceTest pattern) per REQ-MSS-005
- [x] 9. Seeds: stamp `managerUserId: "admin"` on `timesheet-jansen-2026-05`, `expense-jansen-hotel` and `leave-jansen-zomer` (only the submitted seeds) and re-point `orgunit-consultancy.managerId` employee-devries→employee-jansen in hr-seed.json (documented supersession of the archived org-chart-basic seed value) per REQ-MSS-006
- [x] 10. Quality gates: `composer lint` + PHPUnit (php:8.3-cli, full suite, no pre-existing test regressed) + `npm run check:manifest` (Ajv PASS, 0 errors) + `npm run build` all green; verify against the seeded fixtures that the audit reports zero `nl-mss-manager-consistency` violations with the three stamped records evaluating non-vacuously, and no regression in pre-existing checks (run `occ hrmq:rules:audit` when a live instance is available, else the standalone RuleEngine-against-seed-fixtures verification)

Acceptance criteria (plain reminders, not tasks):
- `managerUserId` is a plain nullable string on exactly three schemas — never a `$ref`, never required, never on Payslip/AttendanceRecord/SickLeaveCase
- severity vocabulary stays mandatory/conditional/recommended; this rule is `recommended` and must never trip the mandatory exit gate
- framework stays `hr-org-core` (existing slug) — no SCHEMA.md edit, no new framework
- the predicate is fail-open on absent org data (vacuous) and fail-closed ONLY on a provable mismatch — the exact inverse posture of `nl-org-assignment-consistency`, deliberately (recommended lamp vs mandatory control)
- active-assignment semantics reuse `isCurrentlyActive()` exactly (no endDate, or endDate on/after the audit date)
- no lifecycleActions, no guard, no authorization semantics anywhere — scoping is lists/counts only
- Dashboard widget ids `dash-approve-hours`/`dash-approve-expenses` stay stable (layout references them); new ids use `dash-team-*`/`dash-hr-*` (the sibling verzuim change owns `dash-verzuim-*`/`dash-leave-*`)
- i18n: new labels follow the manifest's existing Dutch-label convention; keys stay stable for `hrmq-i18n-locale-completeness`
