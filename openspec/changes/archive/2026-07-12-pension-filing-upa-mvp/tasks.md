# Tasks — pension-filing-upa-mvp

- [x] 1. Fragment: create `lib/Settings/register.d/hr-pension.json` with the `PensionFiling` schema (properties, required list, defaults, `x-schema-org: schema:Action`, icon `PiggyBankOutline`, version 0.1.0) per REQ-PFU-001
- [x] 2. Lifecycle: add `configuration.x-openregister-lifecycle` (controleren→gecontroleerd guarded, bevestigen, verzenden, heropenen, corrigeren) to `PensionFiling` per REQ-PFU-002
  - transition descriptions document the `submittedDate`/`verzondenDoor` stamping on `verzenden`
- [x] 3. Guard: implement `lib/Lifecycle/PayrollRunApprovedGuard.php` (LifecycleGuardInterface; lazy ObjectService via container; allow only on run status approved/posted/paid; deny with Dutch reason on empty ref, load failure, or any other status) per REQ-PFU-003
  - registered via `Application::register()` (`$c->get()`-constructed with `ContainerInterface` + `IAppConfig`, mirroring the `RuleTestDataSeeder`/`RuleAuditService` DI pattern) — required for OpenRegister's `LifecycleGuardRegistry` to resolve the `requires` FQCN; not explicit in the task list but necessary for the guard to function, same as `NoSelfApprovalGuard`'s registration
- [x] 4. Corpus: add `nl-upa-payrollrun-approved`, `nl-upa-monthly-completeness`, `nl-upa-deadline-alert` to `lib/Standards/rules/payroll.json` (domain reporting, jurisdiction NL, framework `nl-pensioenaangifte`, SIVI sourceUrl); add the framework slug to `lib/Standards/rules/SCHEMA.md` examples; `RuleCatalogue::VERSION` left at `2026-07` (already reflects the current month per SCHEMA.md's "bump on any change" rule — no further bump needed since the loonaangifte-filing-lifecycle change already bumped it this month)
- [x] 5. Audit context: extend `RuleAuditService::audit()` with the `$context['related']` cross-type pre-pass (PayrollRun id/period/status index + approved-run period set; PensionFiling period set) per REQ-PFU-005
- [x] 6. Checks: implement `lib/Standards/Checks/NlPensionFilingChecks.php` (CheckProvider, no SeedsObjects) with the three predicates — reference integrity fail-closed via context, run-period completeness on `PayrollRun`, deadline overdue/14-day window on unsent filings — per REQ-PFU-005
- [x] 7. Unit tests: PHPUnit coverage for the guard (draft denies, approved/posted/paid allow, empty/dangling ref denies) and the three checks (context-driven happy/violating paths, sent-filing never alerts) — extend `tests/Unit/`, bootstrap per `tests/bootstrap.php`
  - the guard implements OpenRegister's `LifecycleGuardInterface`/`GuardResult`, which are not composer dependencies of this repo (sibling-app classes); added a TEST-ONLY conditional stub (`tests/stubs/OpenRegisterLifecycleStub.php`, loaded from `tests/bootstrap.php` only when the real classes are absent) so the guard's real decision logic can be exercised standalone — mirrors the existing OCP-stub safety pattern (stub in bootstrap only, never composer autoload, real classes always win when present)
  - also added `tests/Unit/Service/RuleAuditServiceTest.php` (not explicitly listed in the task, but the `$context['related']` pre-pass added to `RuleAuditService::audit()` in task 5 had zero prior test coverage of that class): drives `audit()` end-to-end through a fake ObjectService double with the exact seeded fixture rows, confirming the pre-pass + predicates compose correctly through the real public API, not just the isolated predicate functions
- [x] 8. Manifest: add `PensionFilings` index page, `PensionFilingDetail` detail page (stats-block, data, related, audit tab, lifecycleActions), and the Loonadministratie menu entry per REQ-PFU-006; `npm run check:manifest` passes
  - Status/Deadline surfaced via a `data` widget (`pf-status`), not a stats-block: confirmed by inspecting the vendored app-manifest-v2 schema + `CnStatsBlockWidget` component source that `stat`/`stats-block` widgets only support count/sum aggregation over a collection and cannot echo a non-numeric field of the current object — the exact same constraint the sibling `LoonaangifteFilingDetail` page's `_note` documents. This mirrors the established, schema-valid workaround rather than inventing a new mechanism.
- [x] 9. Seed data: add the two approved PayrollRun seeds (GL-consistent) and three PensionFiling seeds to `lib/Settings/register.d/hr-seed.json` per REQ-PFU-007 (placeholders only)
- [x] 10. Quality gates: `composer lint` (php -l, zero errors) + PHPUnit in `php:8.3-cli` (48/48 passing) + `npm run check:manifest` (Ajv PASS, 0 errors) + `npm run build` (webpack compiles, pre-existing bundle-size warnings only) all green. `occ hrmq:rules:audit` was not runnable against a live dev container in this headless session (no live Nextcloud/OpenRegister instance available in this worktree run); instead, two equivalent verifications were run directly against the real `RuleCatalogue`/`RuleEngine`/`RuleAuditService`/`NlPensionFilingChecks` classes (no NC/OpenRegister dependency needed for these): (1) a standalone script (php:8.3-cli, composer autoload only) confirmed all three new rule ids are catalogued AND auto-discovered/enforced, and running `RuleEngine::evaluate()` against the exact seed fixture values reproduces the expected result — `pensionfiling-2026-05-spw` flags `nl-upa-deadline-alert`, nothing else flags, both PayrollRun seeds are fully clean (including the pre-existing `xc-payroll-gl-reconciliation`/`xc-withholding-liability-clearing` checks — no regression); (2) `tests/Unit/Service/RuleAuditServiceTest.php` pins the same result through the actual `RuleAuditService::audit()` public API via a fake ObjectService double. No pre-existing check/test regressed (48/48 total suite passing, up from the pre-change 23).

Acceptance criteria (plain reminders, not tasks):
- lifecycle edges exactly as REQ-PFU-002 (no concept→verzonden shortcut; heropenen never from verzonden)
- the guard never allows on error — empty/dangling/unloadable references all deny
- corpus rule ids/framework/sourceUrls exactly as the design.md table; severity vocabulary is mandatory/conditional/recommended only
- fund enum exactly `abp|spw|bpf-bouw|schoonmaak|pfab|pwri`; adding funds later is an enum append, never a rename
- i18n: new page labels/actions use English keys per ADR-007 (Dutch strings go to l10n once `hrmq-i18n-locale-completeness` lands; keep keys stable)
