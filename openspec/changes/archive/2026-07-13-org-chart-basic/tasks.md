# Tasks — org-chart-basic

- [x] 1. Fragment: create `lib/Settings/register.d/hr-org.json` with the `OrgUnit` schema (name/type enum afdeling|team|kostenplaats/self-`$ref` parentUnitId/costCenter/managerId `$ref` Employee/active default true; `x-schema-org: schema:Organization`, icon `SitemapOutline`, version 0.1.0) per REQ-ORG-001
- [x] 2. Fragment: add the `OrgAssignment` schema to the same fragment (employeeId `$ref` Employee + orgUnitId `$ref` OrgUnit required, role, startDate required, endDate nullable; `x-schema-org: schema:EmployeeRole`, icon `AccountArrowRightOutline`, version 0.1.0; NO lifecycle on either schema) per REQ-ORG-002
- [x] 3. Corpus: add `nl-org-assignment-consistency` and `nl-org-unit-cycle` to `lib/Standards/rules/labour.json` (domain labour, jurisdiction NL, framework `hr-org-core`, severity mandatory, machineCheckable true, control-style source per design.md); add the `hr-org-core` framework slug to `lib/Standards/rules/SCHEMA.md` examples; apply SCHEMA.md's `RuleCatalogue::VERSION` bump rule per REQ-ORG-004
- [x] 4. Audit context: extend `RuleAuditService::buildRelatedContext()` with the `OrgUnit` index (`byId` map of `{id, parentUnitId, active}`; degrade-to-empty when the schema is absent, same as the PayrollRun/PensionFiling indexes) per REQ-ORG-005
- [x] 5. Checks: implement `lib/Standards/Checks/NlOrgChecks.php` (CheckProvider, empty seedSpec) — consistency predicate on `OrgAssignment` (endDate>=startDate when present; active assignment must resolve to an `active: true` unit via the context index, fail-closed on dangling refs) and visited-set parent-walk cycle predicate on `OrgUnit` (dangling parent ends the walk, no violation) per REQ-ORG-005
- [x] 6. Unit tests: PHPUnit coverage in `tests/Unit/` for both predicates (coherent/incoherent dates; active-on-inactive-unit violates; ended-on-inactive-unit passes; dangling orgUnitId fails closed; self-parent and two-node cycles violate on every member; deep acyclic chain passes; empty context index) and for the `buildRelatedContext()` OrgUnit index through `RuleAuditService::audit()` with a fake ObjectService double (extend the existing RuleAuditServiceTest pattern)
- [x] 7. Manifest pages: add `OrgUnits` index and `OrgUnitDetail` (data excluding parentUnitId/managerId + related + Child-units and Assignments object-lists + audit sidebar tab; no lifecycleActions) per REQ-ORG-006
- [x] 8. Manifest pages: add `OrgAssignments` index and `OrgAssignmentDetail` (data excluding employeeId/orgUnitId + related + audit sidebar tab), the two `EmployeesGroup` menu children (Organisatie-eenheden / Plaatsingen) and the two `deepLinks` entries per REQ-ORG-006
- [x] 9. EmployeeDetail: add the Assignments `object-list` (filter employeeId=@objectId, sort startDate desc, rowRoute OrgAssignmentDetail, viewAllRoute/viewAllQuery, emptyText) as a new layout row before the Personnel-file Files widget (shift it down); mirror the emp-contracts declaration per REQ-ORG-007
- [x] 10. Seed data: add the three OrgUnit seeds (directie root → consultancy team CC-100 + backoffice CC-200) and three OrgAssignment seeds (jansen/devries open-ended consistent; bakker deliberately endDate<startDate) to `lib/Settings/register.d/hr-seed.json` per REQ-ORG-008
- [x] 11. Quality gates: `composer lint` + PHPUnit (php:8.3-cli, full suite, no pre-existing test regressed) + `npm run check:manifest` (Ajv PASS, 0 errors) + `npm run build` all green; verify against the seeded fixture values that the audit reports exactly one `nl-org-assignment-consistency` violation (bakker), zero `nl-org-unit-cycle` violations, and no regression in pre-existing checks (run `occ hrmq:rules:audit` when a live instance is available, else the standalone RuleEngine-against-seed-fixtures verification the pension change established)

Acceptance criteria (plain reminders, not tasks):
- type enum exactly `afdeling|team|kostenplaats`; adding types later is an enum append, never a rename
- all four reference fields are `$ref`s storing UUIDs (ADR-062 rule 7); seeds reference by slug only
- rule ids/framework/severity exactly as the design.md table; severity vocabulary is mandatory/conditional/recommended only
- the cycle walk MUST carry a visited set (no depth-limit heuristics); a dangling parent is not a cycle
- no lifecycleActions or invented transitions anywhere (neither schema has a lifecycle — PayrollRunDetail precedent)
- i18n: new page labels use the manifest's existing Dutch-label convention; keys stay stable for `hrmq-i18n-locale-completeness`
- D3 tree/exports and OrgChartSnapshot stay OUT — follow-ups `org-chart-visualization` (nc-vue first) and snapshot materialisation
