# Tasks — offboarding-wizard-mvp

- [x] 1. Fragment: add the `Offboarding` schema to `lib/Settings/register.d/hr-onboarding.json` as a sibling of `Onboarding` (properties, required list, defaults, `x-schema-org: schema:Action`, icon `AccountMinus`, version 0.1.0) per REQ-OFB-001
- [x] 2. Lifecycle: add `configuration.x-openregister-lifecycle` (afronding_plannen, eindafrekening_gereedmelden, afronden, annuleren; terminal afgerond/geannuleerd; NO `requires:` guards) to `Offboarding` per REQ-OFB-002, with each forward transition's description naming its checklist/eindafrekening gate fields
- [x] 3. Corpus: add `nl-offboarding-transitievergoeding` (with `parameters`: wageFractionPerServiceYear "1/3", capEur 98000 TODO-commented pending the published 2026 Regeling-indexering figure, capAlternative annual salary, dismissalInitiatedReasons), `nl-offboarding-verlofsaldo-uitbetaling`, `nl-offboarding-getuigschrift` (severity recommended), `nl-offboarding-einddatum-consistentie` to `lib/Standards/rules/labour.json` (framework `bw7-10`, sources BW 7:673/7:641/7:656/7:667, sourceUrl https://wetten.overheid.nl/BWBR0005290) per REQ-OFB-003
- [x] 4. Version: bump `RuleCatalogue::VERSION` `2026-07.5` → `2026-07.6` (next free increment if a parallel change bumped first) per REQ-OFB-003
- [x] 5. Audit context: extend `RuleAuditService::buildRelatedContext()` — add `endDate` to the existing `Employee` index and build the new `LeaveBalance.byEmployeeId` index (entitledHours/bovenwettelijkHours/usedHours rows per employeeId) per REQ-OFB-004
- [x] 6. Checks: implement `lib/Standards/Checks/NlOffboardingChecks.php` (CheckProvider, no SeedsObjects) with the four `Offboarding` predicates — transitievergoeding presence for dismissal-initiated reasons at/past eindafrekening_gereed, verlofsaldo payout at afgerond (skip when no balance resolves), getuigschrift advisory at afgerond, einddatum consistency at afgerond (fail-closed on a dangling employeeId) — per REQ-OFB-004
- [x] 7. Unit tests: PHPUnit coverage for the four predicates (happy/violating paths incl. resignation never transitievergoeding-flags, no-balance skips the verlofsaldo branch, missing endDate counts as einddatum mismatch, dangling employeeId fails closed at afgerond) and for the extended `buildRelatedContext()` through `RuleAuditService::audit()` with a fake ObjectService double — extend `tests/Unit/`, bootstrap per `tests/bootstrap.php`
- [x] 8. Icons: register `AccountMinus` and `AccountMinusOutline` in `src/icons.js` per REQ-OFB-005
- [x] 9. Manifest menu: append the `Offboardings` child (icon `AccountMinusOutline`) to the existing `OnboardingAtsGroup` — group tuple `{id, label, icon, order 106}` stays byte-identical — per REQ-OFB-005
- [x] 10. Manifest pages: add the `Offboardings` index page (columns employeeId/reason/status/lastWorkingDay, filters status+reason, sort lastWorkingDay desc) and the `OffboardingDetail` detail page (Case + Checklist + Eindafrekening data widgets, related, files integration widget, lifecycleActions with exactly the REQ-OFB-002 edges, audit sidebar tab) per REQ-OFB-005; `npm run check:manifest` passes
- [x] 11. Seed data: add the `employee-de-boer` former-employee seed (endDate 2026-05-31, identityDocumentRetainedUntil 2031-12-31, loonheffingenVerklaringOnFile true) and the `offboarding-jansen` (eindafrekening_gereed, missing transitievergoeding) + `offboarding-de-boer` (afgerond clean, lastWorkingDay = endDate) Offboarding seeds to `lib/Settings/register.d/hr-seed.json` per REQ-OFB-006 (placeholders only)
- [x] 12. Quality gates: `composer check:strict` + PHPUnit green, `npm run check:manifest` exits 0, `npm run build` compiles; verify via the audit path (occ `hrmq:rules:audit` on a live instance, or the standalone RuleCatalogue/RuleEngine script fallback) that seed data yields exactly the one expected `nl-offboarding-transitievergoeding` violation on `offboarding-jansen` and no pre-existing check regresses

Acceptance criteria (plain reminders, not tasks):
- lifecycle edges exactly as REQ-OFB-002 (no shortcut edges; `annuleren` never from `afgerond`/`geannuleerd`; NO `requires:` guard on any transition — guard wiring belongs to `hrmq-rule-compliance-enforcement`)
- corpus rule ids/frameworks/sourceUrls exactly as the design.md table; severity vocabulary is mandatory/conditional/recommended only; the transitievergoeding cap constant ships TODO-marked, never invented
- status enum exactly `aangekondigd|afronding_gepland|eindafrekening_gereed|afgerond|geannuleerd`; reason enum exactly `opzegging-werknemer|opzegging-werkgever|einde-contract|pensioen|overlijden|vso`; renames later are breaking and not planned
- no hard dependency on the parallel `asset-management-mvp` change: `assetsIngeleverd` stays a self-contained boolean, no `$ref`/schema-name/rule reads asset objects (design.md D6)
- no custom Vue components in hrmq (Vue logic lives in nc-vue); no eindafrekening computation service, UWV/pensioenfonds submissions or retention timers (named follow-ups)
- i18n: new page labels/actions use English keys per ADR-007 (Dutch strings go to l10n; keep keys stable)
