# Tasks — hr-signals

- [x] 1. Corpus: add `nl-signaal-contract-verloopt` (framework `hr-signals`, severity `recommended`, `parameters.windowDays: 60`) and `nl-aanzegtermijn-bewaking` (framework `bw7-10`, severity `mandatory`, `effectiveDate: 2015-01-01`, `parameters: {minContractMonths: 6, noticeMonths: 1}`) to `lib/Standards/rules/labour.json` per REQ-SIG-001/REQ-SIG-002 (design.md D1)
- [x] 2. Corpus: add `hr-signals` to the framework examples list in `lib/Standards/rules/SCHEMA.md`; bump `RuleCatalogue::VERSION` per REQ-SIG-002 (current at HEAD was `2026-07.8`, not `.5`/`.6` as design assumed — recent fleet activity had already advanced it; bumped to `2026-07.9`)
- [x] 3. Schema: add nullable `aanzegdOn` (date, title + description naming BW 7:668 lid 1) to `EmploymentContract` in `lib/Settings/register.d/hr-objects.json`; schema version 0.1.0 → 0.2.0; register `info.version` bumped in `lib/Settings/hrmq_register.json` per REQ-SIG-003 (current at HEAD was already `0.6.0` — recent merges advanced it past design's assumed `0.5.0` start; bumped to `0.7.0`)
- [x] 4. Context: add `RuleAuditService::buildSignalsContext()` (full-list `contractsByEmployeeId` index incl. object ids, empty-degrade) and wire `$context['signals']` in `audit()` next to the existing builders per REQ-SIG-004 (design.md D4)
- [x] 5. Checks: create `lib/Standards/Checks/NlSignalChecks.php` — window-scoped contract-verloopt predicate (successor probe via the signals context, self-exclusion by id) and the object-local aanzegtermijn predicate (`DateTimeImmutable('today')` convention, vacuous-pass discipline per design.md D2)
- [x] 6. Manifest: add the `dash-expiring-contracts` object-table widget (filter `{type: temporary, endDate: {gte: @today, lte: @today+60d}}`, columns employeeId/endDate/aanzegdOn, rowRoute `EmploymentContractDetail`, viewAll `EmploymentContracts`) + the appended layout row to the `Dashboard` page in `src/manifest.json`; update the page `_note`; cross-reference the 60-day window with the rule's `windowDays` in the widget `_note`; `npm run check:manifest` green per REQ-SIG-005
- [x] 7. Seed: add `contract-devries-tijdelijk` (temporary, 2025-09-01 → 2026-08-01, `aanzegdOn: null`, `awfTariff: high`, wage/hours per design.md Seed Data, jurisdiction-neutral booleans) to `lib/Settings/register.d/hr-seed.json` per REQ-SIG-006
- [x] 8. Unit tests: `tests/Unit/Standards/Checks/NlSignalChecksTest.php` — window edges (day 0/60/61, expired), successor shapes (none / valid successor / earlier sibling / overlapping), aanzeg deadline arithmetic (missed / timely / late-recorded / short term / permanent), empty-context degradation; extend `tests/Unit/Service/RuleAuditServiceTest.php` for the signals index
- [x] 9. Quality gates: `composer lint` green; full PHPUnit suite green (290 tests, no regressions) via `docker run ... php:8.3-cli`; `npm run check:manifest` and `npm run build` green; SPDX + `@spec` tags on new/changed PHP methods (gate-16); NO `x-openregister-notifications` introduced (gate-18 deferral per design, confirmed via diff grep)
- [x] 10. Live verify: no running Nextcloud/OpenRegister dev container is available in this sandboxed builder environment (no docker-compose in this standalone app checkout). Verified equivalently via `RuleAuditServiceTest` fixtures built from the exact `contract-devries-tijdelijk` seed shape: catalogue version `2026-07.9` reported, both new rule ids present in `RuleEngine::checkedRuleIds()`, exactly the two intended violations reported and cleared by a successor, and no pre-existing rule (`nl-awf-laag-hoog-tarief`, `nl-minimumloon-2026`, `nl-minimumuurloon-wet`, `nl-contract-schriftelijk`) regressed. Manifest widget shape validated by `npm run check:manifest` (0 errors). A real `occ hrmq:rules:audit` + Dashboard screenshot pass is still recommended once a live instance is available.

Acceptance criteria (plain reminders, not tasks):
- no proeftijd or WML rule added — the HEAD investigation in the proposal is binding
- one rule id = one severity; advisory tier is `recommended` (no `advisory` enum value)
- predicates stay pure `fn(array $o, array $context): bool`; sibling facts only via `$context['signals']`
- i18n keys ENGLISH per ADR-007 (widget labels are NL display strings in the manifest, matching the existing Dashboard widgets)
