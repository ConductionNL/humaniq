# Tasks — recruiting-ats-basic

- [x] 1. Schema: new fragment `lib/Settings/register.d/hr-ats.json` with `Vacancy` (title/description/department/status/publishedDate/closingDate + `x-openregister-lifecycle` concept→gepubliceerd→gesloten, publiceren stamps publishedDate on the carrying write, terminal gesloten) per REQ-RCV-001; register `lib/Settings/hrmq_register.json` `info.version` 0.3.0 → 0.4.0
- [x] 2. Schema: `Application` in `hr-ats.json` (vacancyId `$ref` Vacancy, candidateName/email/phone/cvFile/motivation PII **inside** the application — no Candidate schema anywhere — + status lifecycle nieuw→screening→gesprek→aanbod→aangenomen/afgewezen with afwijzen from every active stage, rejectedDate + retentionExpiryDate stamped on the afwijzen carrying write, talentPoolOptIn default false) per REQ-RCA-001
- [x] 3. Corpus: new `lib/Standards/rules/privacy.json` (`domain: privacy`, version 2026-07) with `nl-ats-retentie-derivatie` (with `parameters.retentionDays: 28` / `parameters.optInRetentionDays: 365`) and `nl-ats-retentie-verlopen`, both `domain: gdpr-recruitment` / `framework: gdpr` / `jurisdiction: NL`, sourceUrl `https://autoriteitpersoonsgegevens.nl/themas/werk-en-uitkering/sollicitaties` per REQ-RCA-002; bump `RuleCatalogue::VERSION` to `2026-07.1`
- [x] 4. Checks: implement `lib/Standards/Checks/NlAtsChecks.php` (derivatie only on status afgewezen with offsets read from rule `parameters`, NOT hard-coded, vacuous pass otherwise; verlopen against the audit run date on any non-null retentionExpiryDate) per REQ-RCA-003, SPDX docblock included
- [x] 5. Unit tests: PHPUnit coverage for the two ATS checks — correct 28-day derivation, correct 365-day opt-in derivation, wrong/null expiry on afgewezen, vacuous pass on active statuses, expired vs future retentionExpiryDate (extend `tests/Unit/`, bootstrap per `tests/bootstrap.php`)
- [x] 6. Manifest: `OnboardingAtsGroup` menu group (EXACT D6 tuple: id `OnboardingAtsGroup`, label "Onboarding & ATS", icon `AccountPlus`, order 106 — identical to the parallel `onboarding-wizard-mvp` declaration; if the group already exists, add children only) + `Vacancies` index + `VacancyDetail` with lifecycleActions publiceren/sluiten matching `hr-ats.json` action-for-action + `Vacancy` deepLink per REQ-RCV-002/003; register `AccountPlus` + `BriefcaseSearchOutline` + `FileAccountOutline` in `src/icons.js`
- [x] 7. Manifest: `Applications` index (status filter = the pipeline surface; sort candidateName asc) + `ApplicationDetail` (Application + Privacy & retention data widgets, files "CV & motivatiebrief", lifecycleActions screenen/uitnodigen/aanbieden/aannemen/afwijzen, `_note` documenting PII-inside-Application + manual onboarding hand-off) + `Application` deepLink per REQ-RCA-004; `npm run check:manifest` passes
- [x] 8. Seed data: `vacancy-vue-developer` (gepubliceerd) per REQ-RCV-004 and three Applications (nieuw / afgewezen with exact +28-day retentionExpiryDate 2026-06-29 past the reference audit date / aangenomen) per REQ-RCA-005 in `lib/Settings/register.d/hr-seed.json` — placeholder PII only (Voorbeeld names, example.org, nil UUIDs)
- [x] 9. Quality gates: `composer lint` green (per builder protocol); PHPUnit 132/132 green via `docker run php:8.3-cli`; ran the audit logic (`RuleEngine::evaluate`) directly against the seeded Vacancy/Application objects and confirmed exactly the expected violations — `application-voorbeeld-afgewezen` → `nl-ats-retentie-verlopen` (mandatory), zero `nl-ats-retentie-derivatie` violations, nieuw/aangenomen seeds clean — and no pre-existing payroll/labour rule regressed (all 107 catalogue rules still load, `RuleCatalogue::VERSION` = `2026-07.1`)

Acceptance criteria (plain reminders, not tasks):
- no `Candidate` schema, no bespoke tables/services/controllers (the round-1 draft's 6-table plan is superseded — ADR-022/ADR-031)
- lifecycle edges exactly as REQ-RCV-001/REQ-RCA-001 (no re-open on Vacancy, no invented withdraw edge on Application; HR records a withdrawal as afwijzen)
- retention offsets live in rule `parameters` (data-over-code, the WVP `milestoneWeeks` convention)
- menu-group tuple byte-identical to `onboarding-wizard-mvp`'s; re-validate the manifest after any union merge
- gate-28: title + description on every new property; the Application schema description states the delete-one-object-deletes-the-PII posture
- out-of-scope surfaces stay out: no werk.nl/LinkedIn connector stubs, no public routes, no calendar/e-signature/kanban code
- i18n: new page labels/actions use English keys per ADR-007 where keys apply (Dutch display strings follow the existing manifest convention; keep keys stable)
