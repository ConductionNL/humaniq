# Tasks — interview-scheduling

- [x] 1. Schema: add `Interview` to `lib/Settings/register.d/hr-ats.json` (`applicationId` $ref Application, `scheduledStart`/`scheduledEnd`, `interviewers`, `mode`, `location`, `status` + `x-openregister-lifecycle` `scheduled -> completed/cancelled` via `voltooien`/`annuleren`, `calendarEventUid`) per REQ-INTV-001 (design.md D2)
- [x] 2. Config: add `getInterviewCalendarPrincipal()` / `getInterviewCalendarUri()` to `lib/Service/SettingsService.php` (keys `interview_calendar_principal` / `interview_calendar_uri`, empty defaults; docblocks document the keys, the shared-calendar expectation, and the one-way-projection caveat) per REQ-INTV-002 (design.md D6)
- [x] 3. Service: scaffold `lib/Service/InterviewCalendarService.php` with the duck-typed `CalDavBackend` resolution (string class name, `mixed`, try/catch + `method_exists` probes, same five methods as `LeaveCalendarService`) and the per-run availability check → `skipped-no-calendar` per REQ-INTV-006 (design.md D1). Implemented as a thin orchestrator over `InterviewSyncEngine`/`InterviewOrphanReconciler`/`InterviewRepository`/`InterviewIcsRenderer` — a single-responsibility split of what would otherwise be one very large class (kept `check:strict`'s phpmd class-size/complexity/parameter-count rules genuinely green, no baseline padding).
- [x] 4. Service: ICS rendering — hand-built RFC 5545 VCALENDAR/VEVENT (timed DTSTART/DTEND, DTSTAMP, SEQUENCE 0, LOCATION, DESCRIPTION for interviewers as plain text, no ATTENDEE/ORGANIZER) + the §3.3.11 text-escaping helper per REQ-INTV-005 (design.md D5). Lives in `InterviewIcsRenderer`.
- [x] 5. Service: Application/Vacancy resolution for the SUMMARY (`Sollicitatiegesprek — {candidateName} ({vacancyTitle})`, `— kandidaat` fallback) loading only candidateName/vacancy title, never email/phone/cvFile/motivation/talentPoolOptIn/rejectedDate/retentionExpiryDate per REQ-INTV-005 (design.md D5). Lives in `InterviewRepository::resolveSummary()`.
- [x] 6. Service: idempotent upsert — `calendarEventUid` null → derive `hrmq-interview-{uuid}`, probe, create, persist UID back onto the Interview via ObjectService; UID set → probe by stored UID, diff-and-update-only-on-change (no-op avoids etag churn) per REQ-INTV-003 (design.md D3). Lives in `InterviewSyncEngine` + `InterviewRepository::persistCalendarEventUid()` (PUT-semantic field-merge, the `OfferApplicationRepository::save()` idiom).
- [x] 7. Service: cancellation removal + completed-keeps-history + orphan reconciliation (`hrmq-interview-*.ics` URIs with no live Interview id, full live-id set regardless of `--from`, non-`hrmq-interview-` URIs never touched) per REQ-INTV-004 (design.md D4). Cancellation/completion in `InterviewSyncEngine`; orphan reconciliation extracted to `InterviewOrphanReconciler`.
- [x] 8. Command: `lib/Command/InterviewCalendarSyncCommand.php` (`hrmq:interview:sync [--from DATE]`, per-Interview outcome lines + summary, exit 0/1, mirrors `CalendarSyncCommand`) + register in `appinfo/info.xml` `<commands>` per REQ-INTV-007
- [x] 9. Controller: `lib/Controller/InterviewController.php::sync()` guarded by the `isAdminOrHr()` precedent (`PayrollController`/`LoonbeslagController`/`OfferController`), resolve-first RBAC (`OfferController::authorizeApplication()` shape) then invoking `InterviewCalendarService::syncOne()` for the single Interview; route in `appinfo/routes.php` per REQ-INTV-008 (design.md D6). Route is a fixed `POST /api/interviews/sync` with `interviewId` as a body param (`@objectId`) — the established hrmq convention for every guarded manifest action in this app (`request-offer-signature`, `payroll#calculate`, etc.), used instead of design.md's loosely-worded `{id}` path-parameter shorthand so this action matches every other guarded endpoint in the codebase.
- [x] 10. Manifest: `Interviews` (list) + `InterviewDetail` pages under the existing `OnboardingAtsGroup` children (no new top-level menu, ADR-001); `InterviewDetail` data widget (excludes `applicationId`, Related resolves it), `lifecycleActions` exposing `voltooien`/`annuleren`, and the guarded "Sync naar agenda" `api-call` action (`EmploymentContractDetail`/`request-offer-signature` shape) per REQ-INTV-009 (design.md D7)
- [x] 11. Manifest: `ApplicationDetail` gains a FK-scoped "Gesprekken" list widget (`Interview.applicationId`, `rowRoute: InterviewDetail`) per REQ-INTV-009 (design.md D7). Implemented as an `object-list` widget (the established hrmq FK-child-list shape used throughout `EmployeeDetail`/`OrgUnitDetail`/etc.), not a second `related` widget — this manifest only supports one generic `related` panel per page (already in use on `ApplicationDetail` for the outbound Vacancy), so a dedicated, separately-titled child list uses `object-list`, matching the end-user shape design.md described via the `VacancyDetail` "Sollicitaties" analogy.
- [x] 12. Unit tests: `tests/Unit/Service/InterviewCalendarServiceTest.php` with a hand-rolled backend double (no OCA\DAV dev dep) — ICS shape + escaping, stored-UID determinism, create vs update vs unchanged decisions, cancellation removal, completed-keeps-history, orphan reconciliation, privacy invariants (no email/phone/cvFile/motivation/talentPoolOptIn/rejectedDate/retentionExpiryDate/ATTENDEE/ORGANIZER in any rendered ICS), skip paths, PUT-semantic write guard, `syncOne()` coverage
- [x] 13. Unit tests: `InterviewController::sync()` RBAC — non-admin/non-HR request is rejected, admin/HR request invokes the service
- [x] 14. Quality gates: `composer check:strict` ALL CHECKS PASSED (lint/phpcs/phpmd/psalm/phpstan/phpunit, 789 tests = 765 baseline + 24 new, 0 regressions, no baseline padding); SPDX + `@spec` tags on every new/changed PHP method; `npm run check:manifest` PASS (structural lint, 0 issues) on `src/manifest.json`
- [ ] 15. Live verify in an isolated dev environment (not the shared instance, per safety rule): create a shared calendar, set both `interview_calendar_*` config keys, schedule an Interview, run `occ hrmq:interview:sync` — expect one VEVENT; reschedule + re-sync → same UID, updated DTSTART/DTEND; cancel + re-sync → event removed; trigger the manifest "Sync naar agenda" action as an admin/HR user and confirm the same outcome; unset config, re-sync → `skipped-no-calendar` exit 0 (design.md Seed Data / D6). NOT DONE in this job — no isolated dev environment (docker instance) was available/wired in this worktree; the contract above is instead pinned by the PHPUnit suite (task 12/13) against a duck-typed backend double. Left unchecked deliberately, not phantom-ticked.

Acceptance criteria (plain reminders, not tasks):
- no OCP-only write path pretending to upsert — the design's D1 investigation (Option A vs Option B) is binding
- no event ever carries email, phone, cvFile, motivation, talentPoolOptIn, rejectedDate, retentionExpiryDate, or an ATTENDEE/ORGANIZER property
- running the sync twice in a row changes nothing on the second run
- cancelling an Interview and re-syncing removes its calendar event; completing one keeps it
- no OCP/dav info.xml or composer dependency; i18n keys ENGLISH per ADR-007
- no new top-level menu group (ADR-001)
