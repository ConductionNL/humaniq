# Tasks — performance-reviews-mvp

- [x] 1. Schema: new fragment `lib/Settings/register.d/hr-performance.json` with `ReviewCycle` (name/year/type jaargesprek|beoordeling|tussentijds/status/startDate/endDate + `x-openregister-lifecycle` concept→open→gesloten via openen/sluiten, terminal gesloten) per REQ-PRC-001; register `lib/Settings/hrmq_register.json` `info.version` 0.5.0 → 0.6.0
- [x] 2. Schema: `PerformanceReview` in `hr-performance.json` (employeeId `$ref` Employee required, cycleId `$ref` ReviewCycle required, reviewerId `$ref` Employee nullable, status, rating enum onvoldoende…uitstekend nullable, sterktes/ontwikkelpunten/afspraken, `goals` array of {titel, status open|behaald|vervallen, toelichting} **inside** the review — no Goal schema anywhere — besprokenOp/vastgesteldDoor stamped fields, optional denormalized `userId` mirroring Timesheet's verbatim) per REQ-PRV-001
- [x] 3. Lifecycle: declare concept→ingediend→besproken→vastgesteld + heropenen (vastgesteld→besproken), terminal `[]`, with `vaststellen.requires: OCA\Hrmq\Lifecycle\NoSelfApprovalGuard` (reused unchanged — update only the guard's class docblock to name the third consumer) per REQ-PRV-002
- [x] 4. Corpus: add `nl-performance-dossiervorming` to `lib/Standards/rules/labour.json` (framework `bw7-10`, severity `recommended`, machineCheckable, sourceUrl `https://wetten.overheid.nl/BWBR0005290`, effectiveDate 2015-07-01) per REQ-PRV-003; bump `RuleCatalogue::VERSION` `2026-07.5` → `2026-07.6`
- [x] 5. Checks: implement `lib/Standards/Checks/NlPerformanceChecks.php` (predicate evaluates only on status `vastgesteld` — non-null rating AND non-empty afspraken; vacuous pass on all other statuses; SPDX docblock) per REQ-PRV-003
- [x] 6. Unit tests: PHPUnit coverage for the dossiervorming check — complete vastgesteld passes, vastgesteld missing rating violates, vastgesteld missing afspraken violates, concept/ingediend/besproken pass vacuously (extend `tests/Unit/`, bootstrap per `tests/bootstrap.php`)
- [x] 7. Manifest: `EmployeeDetail` gains the `emp-reviews` object-list (PerformanceReview, filter employeeId @objectId, rowRoute PerformanceReviewDetail, NO viewAllRoute per design D6 — verify the renderer accepts an object-list without one) as a full-width row before `emp-files` (which shifts down — the emp-assignments insertion pattern) per REQ-PRV-004
- [x] 8. Manifest: `ReviewCycles` index + `ReviewCycleDetail` (data widget + in-cycle reviews object-list + lifecycleActions openen/sluiten matching `hr-performance.json` action-for-action) as children/sub-pages of the EXISTING `EmployeesGroup` — no new menu group anywhere — + `ReviewCycle` deepLink per REQ-PRC-002
- [x] 9. Manifest: `PerformanceReviewDetail` (data/related widgets, lifecycleActions indienen/bespreken/vaststellen/heropenen with Dutch labels, `_note` documenting goals-inside-review + guard, not a menu child) + `PerformanceReview` deepLink per REQ-PRV-004; `MijnBeoordelingen` under `MijnHrGroup` filtered `userId: @me` per REQ-PRV-005; register `CalendarSyncOutline`/`ClipboardAccountOutline`/`StarCheckOutline` in `src/icons.js`; `npm run check:manifest` passes
- [x] 10. Seed data: `review-cycle-2026` (open) per REQ-PRC-003 and two vastgestelde reviews (`review-jansen-2026` complete with rating goed + goals + userId admin; `review-visser-2026` with rating null → the intended violation) per REQ-PRV-006 in `lib/Settings/register.d/hr-seed.json`
- [x] 11. Quality gates: `composer lint` + PHPUnit green; run `RuleEngine::evaluate` against the seeded objects and confirm exactly one new violation (`nl-performance-dossiervorming` on `review-visser-2026`, severity recommended), zero regressions on pre-existing rules, catalogue loads with `RuleCatalogue::VERSION` = `2026-07.8` (deviation: HEAD was already at `2026-07.7` post-assets when this build started, one .7→.8 higher than design.md's `2026-07.5→.6` baseline — verified against HEAD per builder brief)

Acceptance criteria (plain reminders, not tasks):
- no `Goal` schema, no OKR/9-box/kalibratie/feedback/RewardLink objects, no comp-cycle objects — the `performance-management-advanced` and `comp-planning-cycle` drafts stay separate future changes
- ADR-001 Rule 6: zero new top-level menu entries and zero new menu groups; the dossier row on `EmployeeDetail` is the primary surface
- lifecycle edges exactly as REQ-PRC-001/REQ-PRV-002 (no cycle re-open, no concept→besproken skip); the manifest never claims transitions the schema does not declare
- `NoSelfApprovalGuard` is reused, not forked — no new guard class, no behavioural change; docblock update only
- rating/afspraken stay nullable in the schema; completeness is the recommended-severity audit rule (D8), not a hard gate
- `userId` is a plain denormalized NC-uid string (round-2 pattern), never a `$ref`
- gate-28: title + description on every new property; i18n keys English per ADR-007 where keys apply (Dutch display strings follow the existing manifest convention)
