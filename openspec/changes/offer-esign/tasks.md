# Tasks — offer-esign

- [ ] 1. Schema: bump `Application` to v0.3.0 in `lib/Settings/register.d/hr-ats.json` — add nullable `offerLetterFileId` (integer), `offerSigningRequestId` (string), `offerSigningStatus` (string, enum `PENDING`/`IN_PROGRESS`/`COMPLETED`/`DECLINED`/`CANCELLED`/`EXPIRED`/`skipped-no-docudesk`/`failed`), all with title+description (gate-28); no change to `required` or `x-openregister-lifecycle` per REQ-OFFR-001
- [ ] 2. Config: add `getOfferSigningDeadlineDays()` (config key `offer_signing_deadline_days`, default `'14'`) to `lib/Service/SettingsService.php` per REQ-OFFR-003 (design.md "Config delta")
- [ ] 3. Service: scaffold `lib/Service/OfferEsignService.php` — constructor mirrors `HrDocumentService` (`ContainerInterface`, `IAppManager`, `SettingsService`, `LoggerInterface`); `docudeskAvailable()` probes `IAppManager::isInstalled('docudesk')` + guarded container resolve of `DocumentService`, `TemplateService`, AND `SigningService` per REQ-OFFR-004 (design.md D1)
- [ ] 4. Service: `requestSignature(string $applicationId, ?string $userId=null): array` — resolve the Application, stage guard (`status !== 'aanbod'` → `usage-error` outcome, nothing generated) per REQ-OFFR-002 (design.md D7)
- [ ] 5. Service: idempotency pre-check on `offerSigningStatus` — `PENDING`/`IN_PROGRESS` → best-effort `SigningService::cancelRequest()` (fail-soft, logged) then fall through; `COMPLETED` → `already-signed` no-op outcome; every other value (incl. null) → fall through to a fresh attempt per REQ-OFFR-005 (design.md D6)
- [ ] 6. Service: template selection — re-implement `HrDocumentService::selectTemplate()`'s config-first (`SettingsService::getDocumentsTemplateId('aanbiedingsbrief')`) / discovery-second (`TemplateService::getTemplatesByNamespace('hrmq')` filtered `category === 'aanbiedingsbrief'`) / fail-closed algorithm against the Application subject per REQ-OFFR-002 (design.md D4)
- [ ] 7. Service: `dataRefs = [{register, schema: Application, id}, {register, schema: Vacancy, id}]` (vacancyId off the resolved Application), `adHocData` carries only `employer` block + `document.type: 'aanbiedingsbrief'` — no candidate PII flattened per REQ-OFFR-002 (design.md D3)
- [ ] 8. Service: call `DocumentService::generateDocument()`, verify non-empty `content`, store via `FileService::addFile($applicationId, fileName, content)` where `fileName = "aanbiedingsbrief-{applicationId}-{YYYY-MM-DD}.pdf"` (applicationId, not candidateName, in the filename — AVG-minimisation), capture `$file->getId()` as `offerLetterFileId`; any `\Throwable` at any step → `failed` outcome, `offerSigningStatus: 'failed'` per REQ-OFFR-002 (design.md Context, `FileService::addFile()` finding)
- [ ] 9. Service: call `SigningService::createRequest()` with the VERIFIED real field shape — `documentFileId` (string cast of the file id), `documentName`, `signatureLevel: 'SES'`, `signingMode: 'sequential'`, `deadline` (now + `getOfferSigningDeadlineDays()`), `signers: [{userId: '', displayName: candidateName, email, order: 0}]`, plus provenance (`sourceApp: 'hrmq'`, `subjectRegister`, `subjectSchema: 'Application'`, `subjectId: applicationId`, `subjectLabel: candidateName`, `externalReference: applicationId`, `correlationId: applicationId`); wrap in try/catch → `failed` outcome carrying the real exception message (never let `RuntimeException('No authenticated user')` escape) per REQ-OFFR-003 (design.md D5)
- [ ] 10. Service: on success, persist `offerLetterFileId`/`offerSigningRequestId`/`offerSigningStatus` (from the created request's `status`) onto the Application via `ObjectService::saveObject()` per REQ-OFFR-003
- [ ] 11. Service: `syncSignatureStatus(?string $applicationId=null): array` — resolve target Application(s) (one id, or every Application with `offerSigningRequestId` set and `offerSigningStatus` in `PENDING`/`IN_PROGRESS`), call `SigningService::getRequest($requestId)` (no session needed — read-only), reflect the returned `status` onto `offerSigningStatus` ONLY (never touches `Application.status`, never calls `aannemen`); a `getRequest()` `RuntimeException` (not-found) → log + leave unchanged per REQ-OFFR-006 (design.md D7)
- [ ] 12. Command: `lib/Command/OfferRequestSignatureCommand.php` (`hrmq:offer:request-signature --application <id>`, required option) — docblock/`--help` states the CLI-session limitation (design.md D5) plainly; exit 1 on `failed`/`usage-error`, 0 otherwise per REQ-OFFR-007
- [ ] 13. Command: `lib/Command/OfferSyncSignatureCommand.php` (`hrmq:offer:sync-signature [--application <id>]`) — default scope = every eligible Application; register both commands in `appinfo/info.xml` `<commands>` per REQ-OFFR-007
- [ ] 14. Controller: `lib/Controller/OfferController.php::requestSignature(string $applicationId): JSONResponse` — `#[NoAdminRequired]` + `isAdminOrHr()` (403) + `authorizeApplication()` (`ObjectService::find()` under caller RBAC, unresolvable/unauthorized → 404, BEFORE any docudesk call) + stage guard (400 when not `aanbod`); route `offer#requestSignature` → `POST /api/offer/request-signature` added to `appinfo/routes.php` before the SPA catch-all per REQ-OFFR-007 (design.md D8)
- [ ] 15. Manifest: `ApplicationDetail` gains page-level `actions` with `request-offer-signature` (the `PayslipDetail` `generate-loonstrook` shape: `type: api-call`, `icon: FileDocumentPlusOutline`, `url: /api/offer/request-signature`, `method: POST`, `params: {applicationId: "@objectId"}`, `confirm: true`); "Application" data widget exclude list gains `offerLetterFileId`/`offerSigningRequestId` (`offerSigningStatus` stays visible); `npm run check:manifest` passes per REQ-OFFR-001 / REQ-OFFR-007
- [ ] 16. Seed: `application-voorbeeld-aanbod` (`status: aanbod`, all three new fields null) in `hr-ats.json`, referencing `vacancy-vue-developer`; existing three Application seeds untouched (union, no regression) per REQ-OFFR-001
- [ ] 17. Unit tests: `tests/Unit/Service/OfferEsignServiceTest.php` (mocked container/services per the `HrDocumentServiceTest` setup) — duck-typed skip, stage guard, idempotent supersede/no-op/retry (all 8 status values), the exact `signers`/provenance payload shape asserted against `createRequest()`, `FileService::addFile()->getId()` used (not `getPath()`), the `RuntimeException('No authenticated user')` → `failed` wrap, `syncSignatureStatus()` never writing `Application.status`
- [ ] 18. Unit tests: `OfferController::requestSignature()` RBAC — non-admin/non-HR rejected (403), unknown/unauthorized `applicationId` → 404 before any service call, non-`aanbod` status → 400
- [ ] 19. Quality gates: `composer check:strict` green; in the dev container run the register import, `occ hrmq:offer:request-signature --application application-voorbeeld-aanbod` with docudesk enabled/disabled, the `ApplicationDetail` action end-to-end as an admin/HR user, and `occ hrmq:offer:sync-signature` — confirm the documented CLI-session limitation reproduces exactly as specified (D5) and the manifest-action path completes

Acceptance criteria (plain reminders, not tasks):
- no HTTP path to docudesk anywhere; container-resolved PHP services only, string FQCNs, zero compile-time imports (the leaf's invariant, unchanged)
- `signers`, never a fabricated `signerIds`, is the field passed to `createRequest()`
- neither `requestSignature()` nor `syncSignatureStatus()` ever writes `Application.status` or calls `aannemen` — hiring stays a separate, explicit HR action
- SPDX + `@spec` tags on every new/changed PHP method (gate-16); i18n keys ENGLISH per ADR-007; no Co-Authored-By trailers
