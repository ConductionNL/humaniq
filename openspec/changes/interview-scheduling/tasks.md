# Tasks — interview-scheduling

- [ ] 1. Schema: add `Interview` to `lib/Settings/register.d/hr-ats.json` (`applicationId` $ref Application, `scheduledStart`/`scheduledEnd`, `interviewers`, `mode`, `location`, `status` + `x-openregister-lifecycle` `scheduled -> completed/cancelled` via `voltooien`/`annuleren`, `calendarEventUid`) per REQ-INTV-001 (design.md D2)
- [ ] 2. Config: add `getInterviewCalendarPrincipal()` / `getInterviewCalendarUri()` to `lib/Service/SettingsService.php` (keys `interview_calendar_principal` / `interview_calendar_uri`, empty defaults; docblocks document the keys, the shared-calendar expectation, and the one-way-projection caveat) per REQ-INTV-002 (design.md D6)
- [ ] 3. Service: scaffold `lib/Service/InterviewCalendarService.php` with the duck-typed `CalDavBackend` resolution (string class name, `mixed`, try/catch + `method_exists` probes, same five methods as `LeaveCalendarService`) and the per-run availability check → `skipped-no-calendar` per REQ-INTV-006 (design.md D1)
- [ ] 4. Service: ICS rendering — hand-built RFC 5545 VCALENDAR/VEVENT (timed DTSTART/DTEND, DTSTAMP, SEQUENCE 0, LOCATION, DESCRIPTION for interviewers as plain text, no ATTENDEE/ORGANIZER) + the §3.3.11 text-escaping helper per REQ-INTV-005 (design.md D5)
- [ ] 5. Service: Application/Vacancy resolution for the SUMMARY (`Sollicitatiegesprek — {candidateName} ({vacancyTitle})`, `— kandidaat` fallback) loading only candidateName/vacancy title, never email/phone/cvFile/motivation/talentPoolOptIn/rejectedDate/retentionExpiryDate per REQ-INTV-005 (design.md D5)
- [ ] 6. Service: idempotent upsert — `calendarEventUid` null → derive `hrmq-interview-{uuid}`, probe, create, persist UID back onto the Interview via ObjectService; UID set → probe by stored UID, diff-and-update-only-on-change (no-op avoids etag churn) per REQ-INTV-003 (design.md D3)
- [ ] 7. Service: cancellation removal + completed-keeps-history + orphan reconciliation (`hrmq-interview-*.ics` URIs with no live Interview id, full live-id set regardless of `--from`, non-`hrmq-interview-` URIs never touched) per REQ-INTV-004 (design.md D4)
- [ ] 8. Command: `lib/Command/InterviewCalendarSyncCommand.php` (`hrmq:interview:sync [--from DATE]`, per-Interview outcome lines + summary, exit 0/1, mirrors `CalendarSyncCommand`) + register in `appinfo/info.xml` `<commands>` per REQ-INTV-007
- [ ] 9. Controller: `lib/Controller/InterviewController.php::sync(int $id)` guarded by the `isAdminOrHr()` precedent (`PayrollController`/`LoonbeslagController`), invoking `InterviewCalendarService` for the single Interview; route in `appinfo/routes.php` per REQ-INTV-008 (design.md D6)
- [ ] 10. Manifest: `Interviews` (list) + `InterviewDetail` pages under the existing `OnboardingAtsGroup` children (no new top-level menu, ADR-001); `InterviewDetail` data widget (excludes `applicationId`, Related resolves it), `lifecycleActions` exposing `voltooien`/`annuleren`, and the guarded "Sync naar agenda" `api-call` action (`EmploymentContractDetail` shape) per REQ-INTV-009 (design.md D7)
- [ ] 11. Manifest: `ApplicationDetail` gains a FK-scoped "Gesprekken" list widget (`Interview.applicationId`, `rowRoute: InterviewDetail`), the `VacancyDetail`/"Sollicitaties" shape per REQ-INTV-009 (design.md D7)
- [ ] 12. Unit tests: `tests/Unit/Service/InterviewCalendarServiceTest.php` with a hand-rolled backend double (no OCA\DAV dev dep) — ICS shape + escaping, stored-UID determinism, create vs update vs unchanged decisions, cancellation removal, completed-keeps-history, orphan reconciliation, privacy invariants (no email/phone/cvFile/motivation/talentPoolOptIn/rejectedDate/retentionExpiryDate/ATTENDEE/ORGANIZER in any rendered ICS), skip paths
- [ ] 13. Unit tests: `InterviewController::sync()` RBAC — non-admin/non-HR request is rejected, admin/HR request invokes the service
- [ ] 14. Quality gates: `composer lint` green; full PHPUnit suite green, 0 regressions; SPDX + `@spec` tags on every new/changed PHP method (gate-16); `npm run validate:manifest` (or the project's manifest validator) green on `src/manifest.json`
- [ ] 15. Live verify in an isolated dev environment (not the shared instance, per safety rule): create a shared calendar, set both `interview_calendar_*` config keys, schedule an Interview, run `occ hrmq:interview:sync` — expect one VEVENT; reschedule + re-sync → same UID, updated DTSTART/DTEND; cancel + re-sync → event removed; trigger the manifest "Sync naar agenda" action as an admin/HR user and confirm the same outcome; unset config, re-sync → `skipped-no-calendar` exit 0 (design.md Seed Data / D6)

Acceptance criteria (plain reminders, not tasks):
- no OCP-only write path pretending to upsert — the design's D1 investigation (Option A vs Option B) is binding
- no event ever carries email, phone, cvFile, motivation, talentPoolOptIn, rejectedDate, retentionExpiryDate, or an ATTENDEE/ORGANIZER property
- running the sync twice in a row changes nothing on the second run
- cancelling an Interview and re-syncing removes its calendar event; completing one keeps it
- no OCP/dav info.xml or composer dependency; i18n keys ENGLISH per ADR-007
- no new top-level menu group (ADR-001)
